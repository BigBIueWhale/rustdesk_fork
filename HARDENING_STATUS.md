# Hardening implementation status

This fork is being built **into** the hardened RustDesk specified by
[`requirements.html`](./requirements.html). This file is an honest, living map of
how far the implementation has progressed against that normative spec, and what
each unfinished item needs.

## Working constraints (updated — a build/test loop IS available, in docker)

Earlier iterations assumed **no running of code / no installations** and deferred
everything fail-silent. That premise was **wrong** and has been corrected: a
build/test loop is available **inside disposable docker containers** (the operator
mandate is: compile, install packages, and test *only* in temporary containers,
and never listen on `0.0.0.0`). Empirically:

- **`cargo` 1.75 (the pinned toolchain) + network work**; pure-Rust crates build
  and test directly. The `libs/pake` PAKE — the spec's core, long deferred as
  "RISK-SILENT because the KATs can't run here" — is now **KAT-verified** this way
  (16/16, incl. the published CFRG vector + both fork anchors). The `Cargo.lock`
  **was regenerated via cargo** (the additive `pake` member), so the R-R1/R-A7
  `--locked` invariant is *maintained*, not blocked.
- The **full workspace** still needs native system libs (OpenSSL, libsodium,
  libvpx/yuv/aom, …) that aren't on the host (no passwordless sudo) — but those
  install with `apt` **inside a container**, so the OpenSSL-linked crates
  (`hbb_common` and the two-key secretbox rewrite) are build-verifiable there.
- **The FULL main crate now `cargo check`s in docker** (`scripts/Dockerfile.devcheck`,
  `cargo check --features linux-pkg-config` — distro libvpx/aom/yuv via scrap's
  pkg-config path + a synthesized `libyuv.pc`). This **re-classifies every
  DEFER-BUILD main-crate item below as now-verifiable** — the choke-point cutover,
  the §8 excisions (R-X5/7/8/9/10/11/13/14, R-D4), the §9 main-crate asserts. Only
  the **flutter** build (FRB codegen) and the two-host R-A8/A9 wire tests remain
  out of this loop.

What stays genuinely deferred is only what needs the heavy full-app pipeline or a
real two-host network (the active-attacker R-A8/A9 wire tests), and the items not
yet implemented. Fail-loud source edits still surface at first compile; fail-silent
work (crypto/policy) is now **verified in-container, not committed blind**.

## Landed vs. remaining (quick read)

**Landed — compile/test-verified in docker** (the structured-creation pass plus
the docker-build phase that overturned the false "no-build" premise; pinned 1.75
toolchain, `scripts/verify.sh` is the running gate): the structural monorepo
(1.4.7 + absorbed `hbb_common`, `.git` stripped); the toolchain/version pinning +
`rust-toolchain.toml`; the **complete build harness** (9 scripts) + the R-B2
determinism patch.
- **The CPace PAKE is real, not just a wire format:** the **§10.4 construction**
  (`libs/pake`, 16 KATs — the published draft-21 vector + the fork anchors,
  KAT-VERIFIED); the **two-key secretbox** (`DirectionalCipher`, R-P10) + the
  **choke-point integration** (`server.rs create_tcp_connection` runs
  `run_responder` **unconditionally** — the responder authenticates purely by
  CPace; SignedId/`box_` gone); the per-IP **R-S10** online-guess limiter — all
  wire-tested over loopback (`cpace_it`).
- **§9 assertions:** **R-A1** (keyed-stream-only), **R-A2** (no resume), **R-A3**
  (`set_raw` seal), **R-A4** (startup self-check — policy reads back pinned, PRS
  non-empty, empty BUILTIN/HARD funnels, **and now the live socket surface:
  1×TCP v4:21118, 0×UDP**), **R-A5** (engaged-keys) — all fail-closed and now
  **UNCONDITIONAL** (the `lockdown` feature was retired, R-R2b — they enforce on
  every build). **R-S16** policy funnel (`PINNED_SETTINGS`, `config_it`-tested);
  **R-S2/S5/S6/S7/S9** as noted below (**R-S7** = the load-bearing pre-auth frame
  cap 4 KiB→32 MiB + the 64 MiB decompress bound, the post-key zstd-bomb twin).
- **Direct-only + sovereign:** **R-D4 behavior** (rendezvous loop + LAN emptied),
  **R-D5** (v4-only bind — `listen_any_v4`), **R-SV3/SV4** (version/NAT/heartbeat
  phone-home deleted), **R-F4** (direct-port pin); **R-D3a** (systemd
  confinement); the §8 excisions **R-X1/X2/X3/X4/X6/X11/X12/X14**; **R-SV8** (iOS).

**NOT done** (and why — the load-bearing honesty): the entire **viewer/initiator
side** (`client.rs run_initiator` + the R-S16 viewer-twin) and the **§19 GUI** are
**flutter-gated** (the FRB-generated bridge needs the heavy flutter SDK — installs
but version-skewed, deferred); the **R-R2b** viewer *role* and **R-B6**
(Sciter→flutter) sit behind that same gate — but the **R-R2b lockdown-feature
retirement is now DONE** (the hardening is unconditional on every build; see the
R-R2b row); the *token-absent* completion of the excisions
(**R-X5/7/8/9/10/11/13**, the SignedId proto removal) is entangled with R-B6/R-D4;
**R-R3** dependency audit (4 code-vulns fixed + deny.toml → `cargo audit` green;
the fork-bumps + CI wiring remain); **R-S13** viewer bar, **R-S17** host-key
pin; **R-D6** silent TCP egress (rests on the firewall, not a runtime check); and
the **R-A8/A9 two-host tests + the R-V3 independent audit**, which need real hosts
and an outside auditor. **The project is therefore NOT "implemented in full,
correctly"** — the responder (the exposed box) is comprehensively done and
verified, but the viewer, the GUI, and the external audit remain.

## Status legend

`DONE` · `PARTIAL` (clean part landed, rest noted) · `DEFER-BUILD` (needs a
compile/test loop) · `BLOCK-CARGO` (needs a lockfile regen) · `RISK-SILENT`
(fail-silent; unsafe to do blind) · `TODO`

## Structural baseline & build pinning

| Area | Status | Notes |
|---|---|---|
| §16 monorepo: clone 1.4.7 (`0c86d46`), absorb `hbb_common` (`df6badc`) in-tree, strip `.git`, drop `.gitmodules` | **DONE** | `c2abd3b`; 279→264 Rust files (−15 plugin); no nested repos |
| R-R1 committed `rust-toolchain.toml` (the one pin upstream omits) | **DONE** | `f67a744`; pinned 1.75 |
| R-B7/B9/B10/B11/B12 build harness — all 9 scripts + determinism patch | **DONE** (structure) | `pins.env` + `lib.sh` + `host-provision`/`cleanup` (R-B11) + `online-fetch` (R-B10) + `build-debian`/`build-android`/`build-windows.ps1` + `provision-windows-vm.sh` (R-B7/B8/B9/§12.1/§12.2) + the R-B2 `gen_version` SOURCE_DATE_EPOCH patch (`c5429a4`). Every version pinned + verified in-tree. **Cannot be run/verified under no-run**; SHA-256 digests stay the fail-closed R-B12 sentinel (audited bootstrap is online); the scripts build one binary per platform (R-R2b — no profile split), hwcodec/vram dropped |
| Flutter compile-check harness (NEW — the gateway to viewer/GUI verification) | **DONE — operational** | `scripts/Dockerfile.fluttercheck` (FROM rd-devcheck + pinned Flutter 3.24.5 + `flutter_rust_bridge_codegen` 1.80.1) + `scripts/flutter-verify.sh`: regenerates the FRB Rust bridge from `flutter_ffi.rs`, then **`cargo check --features flutter,linux-pkg-config` is GREEN** on rust 1.75. The feared FRB version-skew is a NON-issue — codegen 1.80.1 installs + runs fine on 1.75 (crate is also 1.80.1). The ONE fork change required: a `Dart_Handle` type alias in `flutter_ffi.rs` (the FRB-1.80.1 cbindgen dummy block references the allo-isolate `Dart_Handle` but the crate doesn't re-export it at root; the generated bridge does `use crate::flutter_ffi::*`, so the alias lands there). `linux-pkg-config` routes scrap/opus's build.rs to the distro libs (rd-devcheck) instead of vcpkg. The Dart-side `generated_bridge.dart` (ffigen) needs `flutter pub get` — deferred to the §19 GUI build. **Unlocks**: the viewer CPace handshake (R-S13/R-S17) + the §19 GUI Rust are now compile-verifiable. |
| Dart/Flutter UI verification harness (NEW — unlocks the §19 GUI sweep + the R-S17 dialogs) | **DONE — operational** | `scripts/dart-verify.sh`: in the rd-fluttercheck image, `flutter pub get` + the FULL FRB codegen (the Dart `generated_bridge.dart` too — 461 KB, works now that `.dart_tool` exists after pub get) + **`flutter analyze lib/` is GREEN — ZERO errors** on the pinned flutter 3.24.5 (the ~238 issues are upstream info/warning lints; `test/` has pre-existing out-of-sync errors, out of scope). So the **Dart UI is now compile-verifiable** — the §19 GUI dead-code sweep (R-G1–G8) and the R-S17 known-hosts seed/mismatch dialogs can be implemented + gated. **R-B12 / R-R1 FINDING (real, documented):** `flutter pub get` under the PINNED flutter 3.24.5 RESOLVES A DIFFERENT `pubspec.lock` than the committed one — it downgrades ~10 packages to the 3.24.5 SDK constraints + adds the `flutter_test`/`leak_tracker` dev-deps — so the committed `flutter/pubspec.lock` is NOT consistent with the pinned flutter, i.e. the Dart side does NOT build from the committed lock as-is (R-R1's "build from the committed lockfile" is broken on the Dart half). The harness RESTORES the committed lock after analyzing (never mutates the pin silently); reconciling it — regenerate the lock under flutter 3.24.5, or align the flutter pin to the lock's flutter — is an open R-B12/R-R3 item. |
| R-R1 keep deps pinned-not-vendored | **DONE** | `Cargo.lock`/`pubspec.lock`/`vcpkg.json` untouched; nothing vendored |
| R-R2/R-R2a prune CI to 3 targets; drop appimage/flatpak/non-Debian | **PARTIAL** | `01bb8a8` deleted fdroid/nightly/playground/clear-cache. `flutter-build.yml` 14-job matrix prune + appimage/flatpak dir removal (coupled to `build.py`+`bump.sh`) = TODO |
| R-B6 drop Sciter | **DEFER-BUILD / BLOCK-CARGO** | non-flutter path is the *default* build (58 `not(feature="flutter")` blocks + the inherited tests run default-features), so a clean cut means making flutter the default/test path — a build-process change; and `sciter-rs` removal is `BLOCK-CARGO` |

### Build-host provisioning — done on THIS machine (the `cte` box we develop on)

The §12.2 Windows-11 KVM build host has been provisioned **on this box** (Ubuntu
24.04.4, the one Linux x86_64 build host of R-B8). State recorded here because it is
host state, not in git:

- **host-provision.sh ran clean** (commits `109e493` + `b82ffb3` made it actually
  work on 24.04): recorded installs = `qemu-system-x86 qemu-utils
  libvirt-daemon-system virtinst swtpm ovmf` (+ `libvirt-clients`/`osinfo-db` as
  deps); the official `docker-ce`/`containerd.io` is REQUIRED (pre-existing per
  `personal_server/README.md`), never `docker.io` (which conflicts). The user was
  added to the `libvirt`+`kvm` groups. All tools verified functional (virt-install
  4.1.0, qemu-img 8.2.2, swtpm 0.7.3, virsh 10.0.0; OVMF incl. Secure-Boot `.ms`
  firmware; `--osinfo win11` resolves via `win-11.xml`).
- **Win11 ISO staged**: `online/win11.iso` (Win11 22H2 x64, 5.2 GB, operator scp'd),
  SHA-256 pinned in `pins.env` (`SHA256_WIN11_ISO=0df2f173…dab22`, R-B12c). It is
  git-ignored (`/online/` + the new `*.iso` guard).
- **STILL BLOCKING a Windows build (two things):** (1) the **VS 2022 Build Tools
  offline layout** (`online/vs-buildtools.layout.tar`, `SHA256_VS_BUILDTOOLS` still
  `$SHA_PENDING`) — operator-supplied; and (2) **provision-windows-vm.sh is a
  skeleton** — audit found it mounts only the ISO (no autounattend / no `./online` /
  no VS-layout mount → an *interactive* install with no offline toolchain path), it
  references a non-existent `autounattend.xml` + guest-setup script, and it omits
  the §12.2-mandated golden-image SHA recording + the VM SSH host-key pin. The guest
  automation must be built (best with the ISO present, now that it is).

The **Debian `.deb`** + **Android `.apk`** builds need none of this (Docker only,
already on the host); the responder-side `verify.sh` gate runs there today.

## The PAKE (§10) — the core

| Area | Status | Notes |
|---|---|---|
| R-P14 `Cpace` wire message (dedicated top-level, not in `Message` union) | **DONE** | `7931abc`; additive proto |
| §10.1–10.4 CPace construction in `libs/pake` (draft-21 CPACE-RISTR255-SHA512) | **DONE — KAT-VERIFIED** | `339b3dd`; construction (lv_cat/o_cat/generator_string/ISK/HKDF/MACs) + R-P14a type-state machine (both roles) + R-P1/2/3/5/6/7/8/9/11/12/14c. **Built & tested in a disposable `rust:1.75-slim` container under the pinned toolchain + pinned lock — all 16 KATs pass**: the CFRG draft-21 published ristretto255 vector (g=`222b6b19…`/ISK_SY=`544199d7…`), fork anchor A (16B sid) and anchor B (32B sid, driven through the full state machine), plus the adversarial set (AD-mismatch, identity, decode, wrong-password→Confirmation, empty-PRS, replay-abort, NFC cross-spelling). No new external crate; lock regen is the single additive `pake` entry (1059→1060, R-R1/R-A7 held). Constraint correction: the "KATs can't run here" premise was false — pure-Rust crates build+test in docker (see memory). |
| R-P2/R-P10 two-key `secretbox` rewrite | **PARTIAL — keying engaged & VERIFIED** | `5ec40de` `DirectionalCipher` (R-P10 fix) + `0108349` `tcp::StreamCipher{Single,Dual}` now backs `FramedStream`/`WsFramedStream`/`Stream` with `set_session_keys(DirectionalKeys)`. **Verified end-to-end on the pinned toolchain**: a CPace handshake over a loopback socket → `set_session_keys` → an app frame travels encrypted and decrypts on the peer both directions (cpace_it, 6 tests). Non-breaking (legacy `set_key` kept for the not-yet-cut SignedId/box_ callers). REMAINING: the choke point calls `set_session_keys` (cutover below), then remove the single-key `set_key`/`Encrypt`/`box_` (R-A6) |
| R-S1/R-P4/R-P14 choke-point integration in `create_tcp_connection` | **PARTIAL — responder + viewer-initiator BOTH WIRED (fail-closed); GUI onboarding COMPLETE** | **A2 (viewer) DONE** — `Client::key_initiator` now runs `cpace::run_initiator_with_transcript` + R-S17 HostIdentity verify + pin-compare at the `Client::start` direct path, fail-closed, with a headless `--pin-host`/`--list-known-hosts`/`--forget-host` known_hosts CLI (verify.sh + flutter-verify green; see the staged recipe steps 2-3). **A3 DONE** — GUI onboarding of a brand-new peer is end-to-end: a no-PRS/wrong-password keying failure → `connect-password-prompt` dialog → set+reconnect; a first-contact unpinned host → `host-not-pinned-prompt` seed dialog (confirm fingerprint) → pin+reconnect. So a fork viewer keys with a fork responder end-to-end, GUI or CLI (a pin MISMATCH stays a loud no-bypass error; the only viewer-keying residue is the dead-`secure_connection`/proto cleanup, step 5). Responder: drivers `5ec40de` + foundations `0108349`/`ef8fa72`, then **`c005da3`: the controlled side now runs `cpace::run_responder` on the direct path** (the `else` of the `if secure` block) with the live PRS, `set_session_keys` on success, fail-closed otherwise (R-A1/R-S1/R-P14c). Compile-verified; the handshake is the cpace_it-proven driver. So the **box refuses unkeyed direct connections** instead of accepting plaintext. `9e65a5b`: **the SignedId↔PublicKey device-identity bootstrap is removed** — CPace now keys UNCONDITIONALLY (no `secure`-gated path, no box_/sign keypair), so the responder side of R-P14/R-P5/§8 is done. `2cf3ad6` did R-S2/R-A2 (authorized only on the keyed edge). REMAINING (viewer = operator's device, intricate + needs a two-host test): `client.rs secure_connection`'s direct branch → `run_initiator` with the entered plaintext password **+ the R-S16 viewer twin** (DONE — a parallel `PeerConfig.password_prs` plaintext field + capture, not overwriting the `password` hash; see the staged recipe step 1 below), the client-side SignedId send removal, the proto SignedId/PublicKey message removal, and `validate_password` deletion (R-S6). **Verified this session — the precise shape of the viewer gap (the load-bearing entanglement):** `Client::_start`'s direct-IP early-returns (`is_ip_str`/`is_domain_port_str`, client.rs:257/272) return an **UNKEYED** `Stream` — NO `secure_connection`, NO `run_initiator` (§4.4 / Appendix C #2 confirmed live); and `secure_connection` (rendezvous-path-only) is STILL the inherited old-SignedId + single-key `set_key` keying (Appendix C #1's bidirectional-reuse bug), with zero CPace. **So a fork viewer cannot key with a fork responder today** (responder runs CPace + emits the R-S17 HostIdentity frame, 5fb04fc; the viewer does neither). Why it is NOT a bounded compile-verifiable wiring: `_start` has **no password parameter** — the inherited flow connects unkeyed, the responder then sends `Hash{salt,challenge}`, and the viewer prompts for the password **AFTER** connecting (`msgbox("input-password")` → `Data::Login`, the flutter substrate). CPace needs the PRS **BEFORE** keying, so the initiator wiring requires **restructuring the viewer password flow to prompt-before-keying** (a flutter-GUI change) on top of the R-S16 viewer-PRS storage, the R-S17 pin-verify + HostIdentity-frame consume, and the proto removal — one interconnected flutter-entangled feature needing the flutter toolchain + a two-host test, hence the deferral. The responder/sovereignty/mediator side is, by contrast, comprehensively done and docker-verified. |

### Viewer CPace handshake — staged implementation recipe (next, now flutter-verifiable)

The flutter compile-check (`scripts/flutter-verify.sh`, `cargo check --features flutter,linux-pkg-config`)
now verifies the viewer Rust, so the gap above is implementable. Structure confirmed by read:
- **Keying point = `Client::start` (client.rs:191)** — it has BOTH `interface` (→ `get_lch()` →
  the per-peer password) AND `Self::_start`'s returned stream. `_start`'s direct-IP branches
  (257/272) return an **UNKEYED** stream, so CPace wiring there is **additive** (no old keying
  to fight — `secure_connection`/SignedId is rendezvous-path-only, and the rendezvous path is
  R-SV4(b)-removed).
- **cpace API** (mirror of the responder, server.rs:214-235): `run_initiator_with_transcript(fs,
  &prs) -> (DirectionalKeys, Transcript)` → `fs.set_session_keys(keys)` → read the responder's
  FIRST post-key frame and `verify_host_identity(&transcript, bytes) -> pk_B` (server.rs emits
  HostIdentity first, 5fb04fc). `fs = stream.as_framed_tcp_mut()`.

Ordered stages (each `cargo check --features flutter,linux-pkg-config` green; behavior needs the
R-A8/A9 two-host test, deferred):
1. **R-S16 viewer-PRS storage twin** — **DONE** (`a separate field, not overwriting password`):
   rather than overwrite `PeerConfig.password` (a hash relied on by many call sites — the ab/recent
   sync, the base64 personal-ab push), a PARALLEL plaintext field `PeerConfig.password_prs: Vec<u8>`
   was added (mirroring the responder's `Config.password_prs`), encrypted at rest via the same
   `encrypt_vec_or_original`/`PASSWORD_ENC_VERSION` path in `store_`/`load` + the manual `Default`
   impl + `#[serde(default)]` (old peer configs load empty). `handle_login_from_ui` (client.rs)
   captures the RAW plaintext into a new in-memory `LoginConfigHandler.password_prs` BEFORE it is
   hashed; the peer-info save path persists it to `config.password_prs` when `remember` (guarded
   non-empty so a hash-only reconnect does NOT wipe a stored PRS; cleared when the password is
   removed). The initiator (stage 2) reads `self.config.password_prs` at connect. verify.sh green.
   NFC normalization is applied by the CPace layer (R-P1), not at storage. **Follow-on (ab/preset
   PRS sourcing):** `handle_hash` (where shared-ab/preset/default plaintext is consumed) runs AFTER
   keying (the responder sends `Hash` only post-CPace), so capturing there is TOO LATE for the
   current connect. The real gap: `key_initiator` sources the PRS ONLY from `config.password_prs`,
   but an ab/preset/default-password peer carries its plaintext in OTHER pre-keying sources —
   `LoginConfigHandler.shared_password` (set in `initialize`, available before connect), the
   `password_preset` arg, and the `OPTION_DEFAULT_CONNECT_PASSWORD` builtin. **`shared_password`
   fallback DONE**: `key_initiator`'s PRS lookup now falls back from `config.password_prs` to
   `shared_password` (strictly additive — empty still fails closed, R-S9), so ab-card peers key.
   REMAINING: the `password_preset` + `OPTION_DEFAULT_CONNECT_PASSWORD` sources are consumed only in
   `handle_hash` (post-keying), so feeding them to `key_initiator` needs extra pre-keying plumbing
   (lower priority). The primary remembered direct-IP path is fully wired.
2. **Direct-path keying** in `Client::start` — **DONE** (A2): a new `Client::key_initiator`
   (the mirror of the responder's `run_responder` block) runs in the `Ok(mut x)` arm of
   `Client::start` when `!stream.is_secured()` — reads the R-S16 PRS from
   `interface.get_lch().config.password_prs`, `run_initiator_with_transcript` →
   `set_session_keys`, fail-closed on empty-PRS (R-S9) / handshake failure / non-TCP, then a
   second `is_secured()` assert (the R-A1/R-S13(b) initiator analog — never proceed unkeyed).
   The verified host pk is surfaced as the connection pk (the UI fingerprint). verify.sh green.
3. **R-S17 viewer verify + pin** — **DONE** (A2): post-key, `key_initiator` reads the
   responder's FIRST frame (`conn.next()`), `verify_host_identity(&transcript, &bytes)` → pk_B
   (fail-closed on a forged/absent host-proof), then the pin compare via
   `host_pin::get_pinned_pk(peer)`: match → ok, **mismatch → abort** (substitution), **no-pin →
   fail-closed (NO silent TOFU)** with an actionable error printing the fingerprint + the
   `--pin-host` command. The headless **known_hosts management CLI** is added (core_main):
   `--pin-host <addr> <fingerprint>` (the out-of-band seed — the `--get-fingerprint` output IS
   the key hex, whitespace-stripped + decoded → `set_pinned_pk`), `--list-known-hosts`,
   `--forget-host <addr>`. So the SSH-known_hosts workflow is COMPLETE and headless: the GUI
   seed/mismatch dialogs (step 4) are now a usability layer, not a prerequisite to function.
4. **Prompt-before-keying (password)** — **DONE**: the connect-time-password SOURCE is wired
   (`Interface::get_connect_password()` reads the lch `connect_password`, the GUI `Session` also
   folds in its construction-time `password` — URI `--password`, AB card; a successful onboarding
   stages the PRS so the save path PERSISTS it). AND the bare-ID flow now prompts BEFORE keying:
   `key_initiator`'s R-S9 (no-PRS) / wrong-password failures are caught in
   `on_establish_connection_error` (flutter) → a `connect-password-prompt` msgbox → the new
   `enterConnectPasswordDialog` → `bind.sessionSetConnectPassword` (FFI →
   `Session::set_connect_password_and_reconnect`: stores the lch `connect_password` + `remember`,
   then `reconnect`) → the reconnect keys with it. So a brand-new peer reached by the main
   "type-ID, click-connect" flow now gets a password dialog and keys (no CLI needed for the
   password). flutter-verify + dart-verify green.
   **R-G5 first-connect pin SEED dialog — DONE**: `key_initiator` now stashes the verified host
   key in the lch `pending_host_pk` on the no-pin abort; `on_establish_connection_error` routes
   the "is not pinned" error to a `host-not-pinned-prompt` msgbox → the new `hostNotPinnedDialog`
   (shows the fingerprint to confirm out-of-band) → `bind.sessionPinHost` (FFI →
   `Session::set_pin_host_and_reconnect`: `host_pin::set_pinned_pk(id, stashed_pk)` then
   `reconnect`) → the reconnect's pin compare matches and keying proceeds. So GUI onboarding of a
   brand-new peer is COMPLETE end-to-end (password dialog → pin-seed dialog → connected), no CLI.
   A pin MISMATCH deliberately does NOT get a seed dialog — it stays the loud `key_initiator`
   error (no easy bypass; the operator must `--forget-host` to re-pin a legitimately re-keyed
   box). flutter-verify + dart-verify green. (A dedicated mismatch GUI panel + the manage/forget
   view are an optional §19 polish; the CLI `--forget-host`/`--list-known-hosts` cover them.)
5. **Cleanup** (R-P5/R-SV4(b)) — **DONE** (`3ce45e1` + the excision commit). Two steps:
   (1) `Client::_start` made DIRECT-IP ONLY — its rendezvous fall-through is replaced by a
   fail-closed bail (a non-IP/host:port peer has no path; the responder runs no mediator and
   `get_rendezvous_server` returns nothing, R-SV4), which made the entire initiator-side
   rendezvous cluster UNREACHABLE. (2) That dead cluster is DELETED (678 lines): `_start_inner`,
   `connect`, `request_relay`, `create_relay`, `secure_connection` (the old SignedId+single-
   `set_key` keying), and the module-level `test_udp_uat`/`udp_nat_connect`; plus 11 now-unused
   imports; plus the `SignedId`/`PublicKey` **proto messages** (message.proto, `reserved 3, 4;`) —
   both are now zero-ref in code, so R-P5 is a **hard verify.sh gate** (`SignedId|set_public_key|
   message::Union::PublicKey` absent; only `//` doc comments naming SignedId survive). The
   responder's handling went earlier (9e65a5b). verify.sh + flutter-verify BOTH green; `_start`'s
   only reachable callers (io_loop / port_forward / cli) pass direct IPs, and a non-direct peer
   already failed at the empty rendezvous, so the bail is outcome-equivalent + explicit.
   **RESIDUE (separate, smaller):** `validate_password` (R-S6); the now-dead online-status helpers
   `query_online_states`/`create_online_stream`/`start_listening` (flagged unused, not flutter-
   called — a follow-on dead-code sweep); `mod kcp_stream`/`udp_nat_connect`-adjacent KCP bits.

## Excisions (§8)

| Req | Status | Notes |
|---|---|---|
| R-X2 native-plugin loader (`src/plugin/`, `plugin_framework`) | **DONE** | `f91dcb9`; 15 files + all 8-file refs removed, zero R-A6 tokens, behavior-neutral (was gated off) |
| R-X6 Android manifest (legacy-storage perms, fake-boot broadcast, `exported`, `allowBackup`, cleartext-deny) | **DONE** | `d4cb686` |
| R-X6 `FloatingWindowService` + `SYSTEM_ALERT_WINDOW` cut | **DONE** | `f8ddac8`; perms 12→9 |
| R-X6 deep-link config/password WRITE authorities (Dart) | **PARTIAL** | `198910d`; write authorities gone. Connect-path `?key=`/`?password=` confirmation + Rust `client.rs` adoption + D-Bus/WM/`_url` transports = `DEFER-BUILD` (Rust-entangled) |
| R-X4 trust-anchor override removal (CLI gadgets + `get_key()`) | **PARTIAL** | `201b13c` excised `--remove`/`--import-config`/`--config` + `import_config()` (R-A6 tokens zero); `59750e7` made `get_key()` return the baked `RS_PUB_KEY` unconditionally — every runtime override (the `"key"` option, IPC blob, Windows exe-name spoof) ignored, on every platform. Remainder (`mod custom_server` + its HKLM/registry/`naming`-bin callers, the Windows `get_license_from_exe_name`, `--set-id`/`--assign`/`--deploy`) = `DEFER-BUILD` (entangled w/ R-D4 account removal + Windows registry) |
| R-X1 auto-updater fetch-and-run RCE | **PARTIAL** | `26125a1`; the whole chain removed — `updater.rs` + `mod updater` + the 3 callers (mediator/ipc/flutter_ffi) + the general & macOS `--update` apply-handlers + the Flutter download-new-version/update-me/extract-update-dmg keys. `crate::updater`/`"download-new-version"`/`"update-me"` = zero. Residual: the now-unreachable platform appliers (`windows.rs update_to`/`update_me`, `macos.rs update_from_dmg`) still hold the last 2 `"--update"` tokens — Windows clean-unused removal + macOS Apple-source = `DEFER-BUILD` |
| R-X3 server-list / config re-home twins (all 3) | **DONE** (behavior) | All 3 re-home writes neutralized on every build: `handle_config_options` (sync.rs, `c658967`), the `TestNatResponse.cu` rewrite (common.rs, `557982a`), and the mediator `ConfigureUpdate` arm (`28c328e`). Full *token*-absence (R-A6 grep) of `mod sync`/NAT-probe/mediator comes with R-D4's cfg-gating; the behavior is closed now, defense-in-depth |
| R-X12 capture-backend pin (forced-display-server env knob) | **PARTIAL** | `340f9b4`; the `RUSTDESK_FORCED_DISPLAY_SERVER` runtime override removed (R-A6 token zero). The structural twin — drop the `wayland` scrap feature + gate the ~20 ungated `Display::WAYLAND`/`Capturer::WAYLAND` refs in `scrap/common/linux.rs` + pin `is_x11()` true — is a coordinated scrap change = `DEFER-BUILD` |
| R-X5 LAN · R-X7 OTP/TOTP · R-X8 terminal · R-X9 Win run-mode/elevation · R-X10 Linux run-mode · R-X11 gtk_sudo · R-X13 uinput/rdp_input · R-X14 os_login PAM | **R-X11 DONE; R-X13 + R-X5 + R-X14 + R-X8-belt NEUTRALIZED; rest DEFER-BUILD** | **R-X8 belt done** (`c3444ae`): the controlled (`lockdown`) build structurally **refuses the `LoginRequest.Terminal` arm** (a `#[cfg(feature="lockdown")]` twin replies "Terminal is not available" — so there is NO code path that sets `self.terminal`, vs relying on the runtime pin) and **ignores `TerminalAction`** (handler gated out) — the root PTY is unreachable on the exposed box independent of the enable-terminal=N pin. Gated on the *existing* `lockdown` axis (not a new feature). REMAINING (R-X8 primary closure): compile out `terminal_service`/`terminal_helper` themselves — a ~20-site connection.rs feature-gate, a dedicated follow-on. **R-X13 core done** (`b1e9522`): `start_uinput_service()` + its unconditional service-entry call removed — the dormant world-mode `_uinput_*` cross-uid sockets the X11 `--server` never used are no longer created (R-S11a surface shrinks to `_service` alone); `wayland_use_uinput()`/`wayland_use_rdp_input()` pinned constant-false so XTEST/enigo is the sole injector by construction (R-X12). uinput.rs/rdp_input.rs stay buildable (§14) but unreachable from the service entry — R-X13's CI bar is call-graph unreachability; the full module compile-out is a follow-on. Hard-gated (`start_uinput_service` absent), compile-verified off+on. **R-X11 done** (`8fb2167`): the **gtk_sudo interactive-elevation cluster is excised** — `src/platform/gtk_sudo.rs` + its `pub mod` decl + the `-gtk-sudo` core_main arm + `run_cmds_privileged` all removed; the two service install/uninstall callers now use a non-elevated status runner (effective only in the root systemd-service context, a no-op on the per-user UI — the .deb + systemctl own lifecycle), and `check_super_user_permission` returns `is_root()` instead of driving an elevation prompt. R-A6 tokens (`gtk_sudo`/`run_cmds_privileged`/`-gtk-sudo`) = 0, **hard-gated in verify.sh**, compile-verified lockdown off+on. `6920db9` stops the LAN UDP discovery *spawn* (no pre-auth UDP surface), and the dead `lan::start_listening` responder FN ITSELF — the `0.0.0.0:21119` pong-er that disclosed mac/id/hostname/username/platform to any pinger — is now REMOVED OUTRIGHT (this commit, R-SV1: structurally absent, not merely unspawned; 'no second listener' is now a code fact, strengthening R-A4). The querier `lan::discover` is now a NO-OP STUB — its broadcast machinery (`send_query` / the broadcast sockets / `wait_response` / `spawn_wait_responses` / `handle_received_peers` + the shared helpers, ~240 lines) is EXCISED (this commit), killing the LIVE LAN UDP broadcast that fired whenever the flutter Discovered tab opened (`disable-discovery-panel` is NOT pinned, so that tab was enabled and the broadcast reachable). The `discover` no-op stub + `send_wol` (R-G6) stay, so full `mod lan` token-absence still pends the cross-harness UI sweep (the flutter `mainDiscover`/`peers_view` trigger + the sciter binding + a `send_wol` relocation); `0685c28` strips the peer-supplied `os_login` so no PAM/X-session login runs on the peer's behalf (R-X14/R-S18). Full token-absence (`mod lan`, the linux_desktop_manager PAM code, the os_login proto field) pends the ui.rs/flutter_ffi sweep + R-D4. The rest are entangled with R-D4 / R-B6 (Sciter) / each other, need semantic reworks, or `BLOCK-CARGO` dep drops (`totp-rs`, `impersonate_system`) — compile-verifiable as de-tangled. **R-X7 (2FA/TOTP) excision — investigated (e817c51), now EXECUTING:** part 1 (`dbbb152`) removed the flutter 2FA UI (−392); part 2a (`6c4d2ac`) removed the 2FA/Telegram-bot setup FFI (`flutter_ffi`'s `main_generate2fa`/`main_verify2fa`/`main_has_valid_2fa_sync`/`main_verify_bot`/`main_has_valid_bot_sync` + their `web/bridge.dart` stub mirrors); part 2b (this commit) excised the **responder machinery** from connection.rs — the `require_2fa` field+init, the `send_logon_response` 2FA gate, the `Auth2fa` handler arm, the trusted-device 2FA bypass + the `enable_trusted_devices` gate, and the raii `Session.tfa`/`set_session_2fa` state — behaviour-preserving (require_2fa was always None ⇒ every branch unreachable), verify.sh-green behind a new hard gate `R-X7 responder 2FA machinery` (`require_2fa|set_session_2fa` absent). **CORRECTION to 6c4d2ac's split:** the remainder is ALL Sciter-coupled (un-verifiable here, R-B6), not Linux-verifiable — the viewer sender `ui_session_interface::send2fa` (which writes `set_auth_2fa(Auth2FA)`) is called by `ui/remote.rs:515` + `ui/common.tis:272`, and `ui_interface.rs`'s `has_valid_2fa`/`generate2fa`/`verify2fa`/`has_valid_bot`/`verify_bot` by `ui.rs`, so the **proto `Auth2FA`/`auth_2fa = 27`**, `src/auth_2fa.rs` + `mod auth_2fa`, and the `totp-rs` dep (R-R3) can drop only with the Sciter UI's 2FA. (`flutter_ffi::session_send2fa` is now uncalled — dbbb152 removed its dialog — but it forwards to the still-live `send2fa`, so it goes with that removal.) **Background + full surface:** 2FA is PINNED-OFF-DEAD — `"2fa"` ∈ PINNED_SETTINGS = `""` (config.rs:3308), so `auth_2fa::get_2fa(None)` → `None` → the responder's `require_2fa` (connection.rs:411) is ALWAYS false, and the pin makes the GUI unable to set a secret (`is_option_can_save` rejects it). R-G4 (spec) also wants the `Auth2FA` proto field gone. Full surface to remove together: **proto** `message Auth2FA` + `auth_2fa = 27` (message.proto:1029); **Rust** the responder gate+handler (connection.rs:411 `require_2fa`, 2387 `Union::Auth2fa`, the `tfa` session state), the viewer sender (`ui_session_interface.rs:1405 set_auth_2fa`), `ui_interface.rs` helpers (`get_2fa`/`generate2fa`/`verify2fa`/`TelegramBot`) + their `flutter_ffi` exports (`main_has_valid_2fa`/`main_generate_2fa`/`main_verify_2fa`/`session_send_2fa`), `mod auth_2fa` + `src/auth_2fa.rs` (drops the `totp-rs` dep, R-R3); **flutter** `change2fa`+`enter2FaDialog` (dialog.dart), the `'input-2fa'` dispatch (model.dart:910) + `send2FA` (model.dart:3912), the "Enable 2FA"/"Telegram bot" toggles (desktop_setting_page ~860, settings_page mobile ~284). **COUPLING to honor:** `Dialog2FaField` (dialog.dart:469) is ALSO used by the ACCOUNT login (login.dart:714, R-G4 account UI) — KEEP it (remove with the account-UI sweep). Add a verify.sh gate `Auth2FA|set_auth_2fa|fn get_2fa` + a dart-verify gate. Trusted-devices (`enable-trusted-devices`="N", pinned) + the `hwid` field are the SAME pinned-off-dead pattern — fold in. **Flutter trusted-devices UI removed** (this commit): the fully-orphaned `_ManageTrustedDevices`/`manageTrustedDeviceDialog`/`TrustedDevice`/`trustedDevicesTable`/`confrimDeleteTrustedDevicesDialog` cluster (settings_page + dialog.dart, ~200 lines — uncalled once the responder enforcement went in part 2b) + the dead `kOptionEnableTrustedDevices` const are removed; dart-verify green. The flutter FFI (`main_{get,remove,clear}_trusted_devices` + `session_get_enable_trusted_devices`) is removed too (follow-on commit, flutter-verify green). Backend DEFERS (still present, dormant): the `ui_interface` wrappers + the `ipc` trusted-devices protocol (the `{Remove,Clear}TrustedDevices` Data variants/handlers, the `trusted-devices` config-value arm, the `get/remove/clear_trusted_devices` wrappers) are now removed too (follow-on commit, verify.sh + flutter-verify green); the `Config` storage (the `trusted_devices` field + `{get,set,add,remove,clear}_trusted_devices` + the `TrustedDevice` struct + `OPTION_ENABLE_TRUSTED_DEVICES` from KEYS/PINNED_SETTINGS + the config_it lockdown KAT entry + the two password-reset clear calls) is removed too (follow-on commit, verify.sh + flutter-verify green); only the proto `LoginResponse.enable_trusted_devices`(=3) + `LoginRequest.hwid` proto fields, and the sciter `get_enable_trusted_devices` (remote.rs/msgbox.tis) — the last is R-B6-coupled. The R-G4 proto deletions (`os_login`/`OSLogin`, `hwid`, `ElevationRequest*`, `SwitchSides*`, `LoginRequest.password`, `Hash`) are each gated on excising their feature first: os_login = the Windows desktop/terminal OS-login (connection.rs:2180+, partly Windows-gated, un-verifiable here); Elevation = the UAC elevation (io_loop.rs:929 sender + connection.rs:3113 handler, LIVE); SwitchSides = the role-swap flow (the SwitchSidesResponse password-bypass/2FA-skip resume was already deleted by R-A2/2cf3ad6 — connection.rs:2375 ignores it; this is the R-A6/R-G4 token-absence excision of the rest, staged top-down): **part 1 done (this commit) — the Flutter triggers are REMOVED** (the toolbar "Switch Sides" `TTextMenu`→`showConfirmSwitchSidesDialog`, the `switch_back` event handler, and the CM-side switch-back button + `handleSwitchBack`, all dart-verify green; only the FRB binding + web stub + dead `fromSwitch`/`switchUuid` plumbing remain). **part 2a done (this commit) — the FFI exports are REMOVED**: `session_switch_sides` + `cm_switch_back` (flutter_ffi.rs) + their `web/bridge.dart` stubs deleted; FRB Rust-bridge regen + `cargo check --features flutter` (flutter-verify) and the dart bridge regen + analyze (dart-verify) both GREEN. The now-uncalled Rust `ui_session_interface::switch_sides` + `ui_cm_interface::switch_back` impls stay (dead, removed with the part-3 core). **part 2b done (this commit) — the `switch_uuid` plumbing is REMOVED**: the `switch_uuid` param is dropped from `session_add`/`session_add_sync` (flutter.rs/flutter_ffi.rs — `lc.initialize` now gets `None`) and the `switchUuid` window plumbing is gone across common.dart (the `--switch_uuid` CLI parse + forward), multi_window_manager.dart (newSession/newRemoteDesktop/newViewCamera), remote_tab_page.dart, remote_page.dart, model.dart, and the web/bridge.dart `sessionAddSync` stub (8 files, zero residual `switchUuid`/`switch_uuid`); flutter-verify + dart-verify both GREEN. **So the entire Flutter side of switch-sides is now excised.** **part 3a done (this commit) — the Rust REFERENCES are all removed** (−263 across ipc.rs/connection.rs/client.rs/io_loop.rs/ui_session_interface.rs/ui_cm_interface.rs/core_main.rs): the 3 ipc `Data` variants + handlers, the connection.rs statics/helpers/`SwitchSidesBack` handler/`SwitchSidesRequest` misc-handler(run_me spawn)/the R-A2-neutered `SwitchSidesResponse` arm, client.rs `consume_local_switch_sides_uuid`+`send_switch_login_request`+the handle_hash switch_uuid block+`allow/consume_switch_back_*`+`switch_back_allowed`, io_loop.rs `SwitchBack` handler, ui_session_interface flutter `switch_sides`, ui_cm_interface `switch_back`, core_main `--switch_uuid`. KEPT (R-B6/shared-API, dead): `lc.switch_uuid`+`lc.initialize` param (sciter caller ui/remote.rs:619), the non-flutter `switch_sides` `{}` stub, the `switch_back` trait method + flutter/sciter impls (push a now-dead event). verify.sh + flutter-verify both GREEN. **part 3b done (this commit) — SWITCH-SIDES IS FULLY EXCISED.** The message.proto deletion landed (the 3 messages `SwitchSidesRequest`/`SwitchSidesResponse`/`SwitchBack` + the Misc `switch_sides_request=21`/`switch_back=22` + Message `switch_sides_response=22` Union arms removed; protoc regenerates message.rs without them) and a new R-A6 hard gate (`SwitchSides|SwitchBack` → 0 in both the Rust src AND message.proto, case-sensitive so the R-B6 lowercase `switch_sides`/`switch_back` stubs are unaffected) CI-proves it; verify.sh + flutter-verify both GREEN. So the SwitchSidesResponse password-bypass/2FA-skip class (R-S2) is now structurally absent end-to-end across proto + Rust + flutter (5 commits: parts 1, 2a, 2b, 3a, 3b). The only switch-sides residue left is the deliberately-kept R-B6 sciter API (`lc.switch_uuid` dead-None, the `switch_sides` `{}` stub, the `switch_back` trait+impls pushing a dead event) — these go with the Sciter UI removal. (Original scope note: part 3 = the Rust core + proto, a ~30-site, both-harness atomic excision — it touches LIVE connection code + spawns a process, so it is a dedicated focused effort, NOT a tail-of-turn change). **COMPLETE site map (mapped, not yet executed):** THREE proto messages, not two — `SwitchSidesRequest`(switch_sides_request=21, Misc), `SwitchSidesResponse`(switch_sides_response=22, Message Union), and `SwitchBack`(a `misc::Union::SwitchBack` arm, io_loop.rs:1920). **ipc.rs**: the 3 `Data` variants `SwitchSidesRequest(String)`/`SwitchSidesUuid`/`SwitchSidesBack` (364-370) + their handlers (958-980). **connection.rs**: the `SWITCH_SIDES_UUID`/`PENDING_SWITCH_SIDES_UUID` statics (89-90), the `ipc::Data::SwitchSidesBack` handler (680), the already-neutered `message::Union::SwitchSidesResponse` ignore arm (2367-2375, R-A2), the LIVE `misc::Union::SwitchSidesRequest` responder handler (3067 — it `insert_pending_switch_sides_uuid` + `run_me(["--connect",id,"--switch_uuid",uuid])` spawns a new process + on_close), and the `insert_switch_sides_uuid`/`insert_pending_switch_sides_uuid`/`remove_pending_switch_sides_uuid` helpers (4923-4945). **client.rs**: `lc.switch_uuid` field+assignment (1252/1377) + the `lc.initialize` param (1290, now always None), `allow_switch_back_once` (1392), `consume_switch_back_permission`, `consume_local_switch_sides_uuid` (2921, flutter), the now-dead `handle_hash` switch_uuid block (2972-2982, flutter — `lc.switch_uuid` is always None), `send_switch_login_request` (3173 — `set_switch_sides_response`). **io_loop.rs**: the `misc::Union::SwitchBack` viewer handler (1918-1933 — `consume_switch_back_permission` + `handler.switch_back`). **ui_session_interface.rs**: the flutter `switch_sides` (1497, `set_switch_sides_request`) + the `switch_back` handler method — REMOVE; the non-flutter sciter `switch_sides` `{}` stub (1492) is R-B6, KEEP. **ui_cm_interface.rs**: `switch_back` (466-472, now-dead since part 2a). **core_main.rs**: the native `--switch_uuid` CLI arm (668-670). **message.proto**: the 3 messages + the Misc `switch_back`/`switch_sides_request` + Message `switch_sides_response` Union arms. R-A6 gate `SwitchSidesRequest|SwitchSidesResponse|switch_sides` → 0 (verify.sh + flutter-verify). NOTE `from_switch` (the CM 2FA-skip state) stays — nothing sets it true after R-A2, it is provably-false dead state, and it is NOT in the R-A6 token list (a separate optional cleanup). password/Hash = the login handshakepassword/Hash = the login handshake (the CPace skips the proof, R-S6, but the Hash oracle is still the connection trigger — needs the prompt-flow rework). So R-G4-proto is a multi-feature excision arc, 2FA the cleanest (dead) first step. |

## Secure-by-assertion, policy, role split, deployment, sovereignty, GUI

| Area | Status | Notes |
|---|---|---|
| §9 R-A1–R-A10 runtime/build/test assertions | **PARTIAL — gate RUNS** | `a583fd9` `scripts/verify.sh` is the executable §9.3/R-V3 assurance basis: it RUNS the R-A10 KATs (pake 16 + cpace_it 5 + config_it), compile-checks the main crate (lockdown off/on), and hard-gates the **R-A6** greps for the completed excisions (R-X1/X2/X4-CLI/X6/X12 = 0 tokens) — all green. **R-A5** done (`7394561`); **R-A1 done** (`3e97f37`, lockdown-gated `is_secured()` assert before `Connection::start`); **R-A4 DONE** (`2957396`/`5140124`/`b6df149`): `assert_startup_invariants()` at the service entry refuses to listen unless the policy reads back pinned (verification-method/approve-mode via `get_option`), a permanent password exists (R-S9), AND the BUILTIN_SETTINGS / HARD_SETTINGS funnels are empty (R-S16(d)(iv)(v)); and post-listen `assert_socket_surface()` confirms the live bound-socket surface — exactly **1×TCP v4:21118, 0×UDP of any kind** (a pure `/proc/self/net` parser + policy, unit-tested in `surface_it`; counts LISTEN-state only so a session's ESTABLISHED row never miscounts; the any-UDP rule closes the ephemeral-egress blind spot; non-Linux records *unavailable* per R-A4's Darwin clause) — all fail-closed. REMAINING: only the R-A8/A9 two-host exercise of these asserts; **R-A2 done** (`2cf3ad6`): the is_recent_session 30-s reconnect and the SwitchSidesResponse resume — both set `authorized` without a fresh handshake — are removed, so authorization coincides with the CPace KEYED edge; **R-A3 done** (`ddb5c05`): an assert seals `set_raw` so a keyed stream can't be downgraded to raw. **Update (lockdown retired, R-R2b):** R-A1/R-A3/R-A4 are no longer behind a feature flag — the `lockdown` cfgs are dropped and the asserts compile into every build; the main-crate `cargo check` is now a single unconditional pass (no off/on split). REMAINING: R-A6 turns red→green per excision; R-A8/A9 are the two-host tests |
| R-S16 controlled-policy `PINNED_SETTINGS` funnel | **PARTIAL — config funnel VERIFIED** | `675514b`; (a) `keys::PINNED_SETTINGS` table + (b) `get_option` read funnel + (c) `is_option_can_save` write guard, behind a new `lockdown` feature (empty/no-op when off). **Tested on the pinned 1.75 toolchain** (`config_it` crate, lockdown on): every pinned key returns its policy value and resists override, non-pinned keys unaffected. Was RISK-SILENT (fail-open if wrong) — now behavior-pinned. **R-S16(d)(i) done** (`af15880`): the main-crate `lockdown` feature + `Connection::permission` early-return skipping the `control_permissions` server-push bypass — compile-verified both lockdown on/off. **Update (lockdown retired, R-R2b):** PINNED_SETTINGS and the get_option/is_option_can_save funnel + the `control_permissions`-skip are no longer behind the `lockdown` feature — they are UNCONDITIONAL on every build (R-S16a: "enforced by every artifact, never behind a feature flag"); `config_it` now tests them without the feature. **R-S16(d)(ii) done** (this commit): the runtime SwitchPermission widener handler is removed (a CM toggle can no longer re-widen a pinned-off capability; R-A6-gated). REMAINING: `get_builtin_option` (d)(iv) mirror (only if a KEYS_BUILDIN value is ever pinned), and R-S16's password-storage twin (PRS-at-rest, part of the choke-point cutover) |
| R-S2 FSM collapse · R-S5 `set_raw` seal · R-S7 pre-auth frame cap + decompress bound · R-S9 PRS-at-rest · R-S10 limiter re-key · R-S13 initiator bar · R-S17 host-key pin · R-S18 OS-credential delete | **PARTIAL** | **R-S2 resume paths removed** (`2cf3ad6`, see R-A2); **R-S9 PRS-at-rest** done (`ef8fa72` plaintext-PRS storage + the R-A4 non-empty check). **R-S10 limiter** done (`89bc4e9`): per-IP online-guess limiter in `hbb_common::cpace` (60-s window, ≤10 confirmation failures), checked before the scalar-mult and fed ONLY by R-P3 confirmation mismatches (R-P14c) — wired at the choke point, unit-tested. **R-S6** done (`ab5083a`): the redundant login-time password proof collapses into the PAKE — skipped when CPace-keyed. With SignedId gone (`9e65a5b`), the responder authenticates purely by CPace. **R-S2 password-oracle DELETED (this commit):** `ab5083a` *skipped* the post-key salted-hash proof when keyed; this now *deletes* it structurally — `validate_password` (+ `_plain`/`_storage`/`_preset`) and `verify_h1` are removed, and the login call site reads `if !self.stream.is_secured()` ALONE. That is strictly **more fail-closed**: the old `!is_secured() && !validate_password` would have password-validated an unkeyed peer, whereas the new form rejects an unkeyed stream outright. Because R-A1 (unconditional) refuses unkeyed streams before `Connection::start`, the keyed path is unchanged (always authorized on the CPace edge); only the *unreachable* unkeyed branch flips validate→reject. verify.sh + flutter-verify GREEN; new R-A6 gate `validate_password|verify_h1` → 0. **`is_recent_session` + the SESSIONS recent-session cache DELETED (this commit):** the 30-s resume `is_recent_session` (already gutted to `false` by `2cf3ad6`) + its dead branch in the login chain, and the entire SESSIONS subsystem (the static, the `Session{last_recv_time,random_password}` struct, the `session_last_recv_time` conn field + its recv-loop update / on_open read, `SESSION_TIMEOUT`, and the `raii` `check_remove_session`/`update_or_insert_session` cache ops + their 3 close-handler calls) are removed — the cache's ONLY populator was the just-removed `validate_password`, so it was always empty (write-never). `session_key()`/`SessionKey` stay (the separate `AUTHED_CONNS` active-tracker uses them). The R-A6 gate is widened to `validate_password|verify_h1|is_recent_session` → 0; verify.sh + flutter-verify GREEN. The LONE remaining R-S2 FSM token is the `Hash{salt,challenge}` emission `set_hash` — still the viewer's login trigger, whose removal needs the prompt-flow rework (a dedicated follow-on). **R-S5** set_raw seal done (`ddb5c05`). **R-S7** done (`7e67843`, the load-bearing pre-auth DoS control): the pre-PAKE frame cap (4 KiB — `set_max_packet_length` in `run_responder`/`run_initiator`, the only attacker-reachable parser before keying, closing the ~1 GiB/connection amplification) is RESET to the 32 MiB session ceiling on keying (else legit frames break at 4 KiB), and the post-key twin caps `compress::decompress` output at 64 MiB — rejecting a zstd bomb that the inherited unbounded `zstd::decode_all` would inflate without limit. Both unit-tested (`cpace_it` behavioral frame-cap, `compress_it` bomb-rejection). The kcp C-parser drop (direct-only, both roles TCP) pends R-D4. **R-S17 partial** (`e060f7d`): the headless **`--get-fingerprint`** CLI arm (prints `pk_to_fingerprint(get_key_pair().1)` directly — no daemon, no network — the substitution-defense bootstrap the controlled box was missing, since the GUI `get_fingerprint` is not reachable headless) is added beside `--get-id`; and (`a0829ee`) the **R-SV5-shared address-normalization** function (`hbb_common::address::normalize_address` — lowercase, IDNA to_ascii, port-fill to 21118, IPv4-canonicalize, reject colon/IPv6) is pinned with a **frozen KAT** (`address_it`, 8 tests incl. the spec's `1.2.3.4`≡`1.2.3.4:21118` and `EXAMPLE.com:21118`≡`example.com:21118`), foreclosing the substitution blind spot. And (`5fb04fc`) the **responder's HostIdentity host-proof frame is DONE** — the responder, after keying, emits a dedicated `HostIdentity {pk, sig}` frame (encrypted, first post-keyed frame, NOT a Message-union arm) carrying an Ed25519 signature over `"rustdesk-fork/host-proof/v1" ‖ sid ‖ CI ‖ Ya ‖ Yb` with the box's `get_key_pair()` secret key. The transcript is reconstructed in the cpace driver from the Step messages (no pake change, 16 KATs intact); `build_host_identity`/`verify_host_identity` + a `HostProof` fail-closed error; `cpace_it` 9/9 incl. roundtrip + different-session/tampered/garbage negatives — so a substitute that knows the password but lacks the box's private key is caught, welded to this session (forecloses the tunneled-MITM class). And (this commit) the **R-S17 pin store is now BUILT** — `hbb_common::host_pin` + the isolated `host_pin_it` KAT: a SEPARATE `host_pins.toml` (NOT `PeerConfig`, so a keyed hostile peer cannot seed/overwrite a pin — R-S15), keyed by the **same** `address::normalize_address` the R-SV5 peer list uses (a mismatch would be a substitution blind spot), stored `0o600`, with `get_pinned_pk`/`set_pinned_pk`/`remove_pinned`/`list_pinned`; the KAT verifies round-trip, the `1.2.3.4`≡`:21118` + `EXAMPLE.com`≡`example.com` keying (shared w/ R-SV5), re-pin-in-place, and forget. So the substitution-defense building blocks (`--get-fingerprint`, `normalize_address`, the responder HostIdentity emit, the pin store) are ALL done. **R-S17 viewer-side wiring is DONE** (verified present, ledger was stale): `client.rs` (~360-410) runs CPace → `set_session_keys` → reads the responder's HostIdentity host-proof as the FIRST post-key frame (fail-closed if absent) → `cpace::verify_host_identity` (fail-closed if it doesn't verify) → `host_pin::get_pinned_pk` compare: same key ⇒ proceed; DIFFERENT key ⇒ refuse (substitution/MITM, never auto-re-pin); first-contact ⇒ NO trust-on-first-use — stash `pending_host_pk`, print the fingerprint, fail-closed with the `host-not-pinned-prompt` GUI dialog (R-G5). Seed-accept is wired both ways: GUI `session_pin_host` → `ui_session_interface` → `host_pin::set_pinned_pk(pending_host_pk)` + reconnect, and the `--pin-host <addr> <fp>` CLI (core_main.rs). Building blocks KAT-locked (cpace_it verify_host_identity roundtrip/negatives; host_pin_it round-trip/normalize). The substitution defense is complete end-to-end; only the §9 R-A8/A9 two-host behavioural exercise remains (no local-harness coverage). **R-S13 (the viewer-keying bar) is ALSO DONE** (ledger was stale): the unkeyed direct-IP/domain:port early-returns + the `secure_connection`/`_start_inner`/`request_relay`/`create_relay` machinery are deleted (`Client::_start` is direct-only, fail-closed; the R-P5 verify.sh gate `SignedId|set_public_key|message::Union::PublicKey` proves the device-identity keying is gone), `key_initiator` (client.rs:351) runs the SINGLE mandatory CPace handshake before any Message parse (R-P14, R-S7 pre-key frame cap applies via run_initiator), and the R-A1 initiator assert (client.rs:272, `bail!("R-A1: refusing to proceed on an unkeyed stream (initiator)")`) refuses an unkeyed stream. So the viewer-side keying + substitution defense are BOTH complete. REMAINING: only the R-S7 kcp-parser drop (deferred with R-D4 — both roles are already TCP-only, so the --server binds no UDP). **R-S15 viewer config-integrity DONE (this commit):** the viewer's in-session PeerConfig writes from peer-controlled data — `handle_peer_info`'s `PeerInfoSerde{username,hostname,platform}` (client.rs) and `update_privacy_mode`'s privacy-mode `impl_key` (io_loop.rs) — are funnelled through `hbb_common::config::bound_peer_config_string` (strip control chars + clamp to 256 chars), so a keyed-but-hostile peer can no longer inject unbounded or control-character strings into the viewer's on-disk PeerConfig (Appendix C #19; the initiator twin of the responder R-S11 config-write gate). `get_username` (a transient display read, not a config write) is correctly out of scope. KAT in `config_it/tests/r_s15.rs` (control-strip, length-clamp, unicode-by-char-boundary) + a verify.sh presence gate (`bound_peer_config_string` in client.rs + io_loop.rs); verify.sh + flutter-verify green. **R-S18 viewer os_login/hwid sender REMOVED** (this commit): `create_login_msg` (client.rs) built `os_login: Some(OSLogin{os-username,os-password})` + the `hwid` device fingerprint into the LoginRequest on EVERY connect — the Appendix C #22 client-side leak (a substituted peer, R-S17, harvested the operator's stored OS creds + fingerprint with no interaction). Removed + a new verify.sh hard-gate (`Some(OSLogin` absent); flutter-compile-verified. The responder already ignores os_login (0685c28/R-X14), so this completes the symmetric removal of THIS class. **R-S18 elevation-with-logon sender also REMOVED** (this commit): the viewer's `Data::ElevateWithLogon` arm (io_loop) built `ElevationRequest::set_logon(ElevationRequestWithLogon{username,password})` — peer-supplied OS creds for a Windows `CreateProcessWithLogonW` elevation (the responder side is excised by R-X9); gutted to a no-op, folded into the verify.sh `Some(OSLogin`|`.set_logon(` gate; flutter-compile-verified. REMAINING R-S18: the viewer `set_switch_sides_response` (client.rs) sender, the `OSLogin`/`hwid`/`ElevationRequestWithLogon`/`SwitchSides*` proto-field deletions (message.proto), the persisted os-username/os-password options + their UI (§19/R-G4), and the Windows/PAM responder paths. **Viewer Request-Elevation toolbar menu REMOVED** (this commit): the explicit "Request Elevation" `TTextMenu` (toolbar.dart, gated on `ffi.elevationModel.showRequestMenu`) is deleted — `showRequestElevationDialog` stays (still reached by the host-triggered flow), `showRequestMenu` is now a benign unused getter. NOTE the os-credential *leak* is already closed (the R-S18 `Some(OSLogin`/`.set_logon(` senders are excised), so the rest is R-G4 cleanup of dead UI — a careful focused removal: the model msgbox dispatch cases (`on-uac`/`on-foreground-elevated`→`showOnBlockDialog`, `wait-uac`→`showWaitUacDialog`, `elevation-error`→`showElevationError`, `terminal-admin-login-tip`→`enterUserLoginAndPasswordDialog`), those 4 dialogs + `showRequestElevationDialog`, the `ElevationModel` class + its `onPeerInfo`/`onPortableServiceRunning` wiring (woven with `_handlePortableServiceRunning`), and the os-username/os-password fields in the connect dialog (dialog.dart ~906-1117). **OS Account/Password UI REMOVED** (follow-on commit): the toolbar "OS Account"/"OS Password" `TTextMenu` + its `handleOsPasswordEditIcon`/`handleOsPasswordAction` handlers + the `isEditOsPassword` flag (toolbar.dart) and the two os-credential dialogs `showSetOSPassword` + `showSetOSAccount` (dialog.dart) are all removed — the viewer can no longer enter/store OS credentials to push to the host (dart-verify green). **Host-triggered elevation flow REMOVED** (this commit): the 4 host-triggered elevation dialogs (`showWaitUacDialog`/`showRequestElevationDialog`/`showOnBlockDialog`/`showElevationError`, dialog.dart −243), the 3 model msgbox dispatch cases that invoked them (`on-uac`/`on-foreground-elevated`→block, `wait-uac`→wait, `elevation-error`→error), and the **entire `ElevationModel`** (the `showRequestMenu`/`onPeerInfo`/`onPortableServiceRunning` getter+wiring, its field+init, the `_handlePortableServiceRunning` handler, and the `portable_service_running` event case feeding it) are all removed — the Flutter viewer now has NO Windows-UAC/elevation surface left (dart-verify green; zero `elevationModel`/`ElevationModel` refs). **Viewer os-login dialogs + connect-dialog os-fields REMOVED** (this commit): the 4 host-triggered os-login msgbox dispatch cases (`session-login`/`session-re-login`, `session-login-password`, `terminal-admin-login`, `terminal-admin-login-password`, model.dart) and the two entry dialogs they invoked (`enterUserLoginDialog`/`enterUserLoginAndPasswordDialog`, dialog.dart) are deleted, and the shared `_connectDialog` is simplified to **rd-password-only** — its `osUsernameController`/`osPasswordController` params, the `osAccountWidget` (OS user/pass text fields), the `os-username`/`os-password` `sessionPeerOption` persistence, and the `rememberAccount` logic are all gone (the R-S13/R-S17 `enterPasswordDialog`/`enterConnectPasswordDialog` password paths are preserved). `gFFI.login` drops its `osUsername`/`osPassword` params (forces `''` into the still-present `bind.sessionLogin` FFI). So the Flutter viewer can no longer be prompted to push OS creds to the host — completing the §19/R-G4 UI side of the os-credential class (the leak itself was already closed by the R-S18 sender excision + R-X14 responder strip). New dart-verify gate `R-S18/R-X8 viewer os-login dialog`. STILL REMAINING (R-S18): the Rust `session_login` os_username/os_password FFI params (now always `''` — a flutter-verify signature trim, like the trusted-devices FFI); and the proto/Windows-responder side (the `ElevationRequest*`/`OSLogin`/`SwitchSides*` proto-field deletions + the Windows/PAM responder paths). |
| R-R2b one binary, both roles — harden unconditionally, no build-profile split | **PARTIAL — lockdown feature RETIRED** | Keeps RustDesk's single binary (viewer + `--server` by argv); surface reduced by deletion, policy unconditional. Scripts build one `.deb`/`.apk`/`.exe` with hwcodec/vram dropped (software codec). **The `lockdown` Cargo feature is RETIRED** (this commit): every `#[cfg(feature="lockdown")]` gate is now unconditional and the `not(lockdown)` stubs + the `lockdown`/`hbb_common/lockdown` feature defs are deleted — so PINNED_SETTINGS (R-S16a), the R-A1 unkeyed-stream guard, the R-A3 `set_raw` assert, the R-A4 startup + socket-surface asserts, the R-S16(d)(i) `control_permissions`-skip, and the R-X8 terminal-refusal-by-pin all compile into **every** artifact, never behind a flag. The terminal stays runtime-locked (the enable-terminal=N pin refuses the `LoginRequest.Terminal` arm; `handle_terminal_action` bails fail-closed on a None `terminal_user_token`), code-present per §14/R-F4. The R-S16(d)(ii) runtime `SwitchPermission` widener (the conn-side IPC handler that re-assigned conn.keyboard/clipboard/audio/file/restart/recording/block_input/privacy_mode at runtime, bypassing `permission()`) is **REMOVED** — a CM-sent `Data::SwitchPermission` now falls to the catch-all and is ignored; a new R-A6 hard-gate (`ipc::Data::SwitchPermission` absent) CI-proves it. Verified green in docker (`verify.sh`: 50 unit tests + main-crate `cargo check` + R-A6 greps incl. the new SwitchPermission gate). REMAINING: only the R-R2b viewer *role* (flutter-gated) — the controlled/responder side of R-R2b is done |
| R-F4 direct port pinned to compile-time `21118` | **DONE** | `128d838`; new `config::DIRECT_PORT = 21118`, `get_direct_port` returns it unconditionally (no config read, no rendezvous+2 derivation) — load-bearing for the §10.4 CPace `CI` KAT be16(21118)=527e. The orphaned `direct-access-port` UI setting is a §19 cleanup |
| R-D3a systemd confinement of the root service | **DONE** | `64e11b4`; the exact R-D3a directive set (CapabilityBoundingSet, RestrictAddressFamilies=AF_UNIX AF_INET, ProtectKernel*, SystemCallFilter, …), NoNewPrivileges deliberately omitted, MemoryDenyWriteExecute documented-but-disabled pending runtime validation |
| R-D1/D2/D4–D8 deployment (direct-only, v4-only, silent egress, config pins) | **PARTIAL — direct-only BEHAVIOR live (VERIFIED)** | `6920db9`; `start_all` no longer connects to any rendezvous server (rendezvous loop emptied — closes the phone-home that the R-S16 `custom-rendezvous-server=""` pin alone could NOT, since `get_rendezvous_servers()` defaults to the built-in upstream) and no longer starts LAN discovery. The box is reachable only by direct connection. `59d4983`: the direct listener is now **unconditional** — no longer gated on the `direct-server` option, so the box's only inbound path (and the CPace responder) reliably starts (R-D4/R-F4; the spec keeps direct-server out of PINNED_SETTINGS for this). Compile-verified. **R-D5 done** (`b6df149`): the direct listener now binds **v4-only** via `listen_any_v4` (the v4 body used unconditionally, not the dual-stack `[::]:21118` socket) — IPv6 unreachability is a property of the binary, not a host sysctl / `ip6tables` rule that can drift, and R-A4 asserts it post-listen. **R-D4 lift Stage 1 done** (this commit): a new module-level `start_direct_only()` is the service entry — the minimal KEEP path (assert_startup_invariants + check_zombie + new_server + spawn `direct_server` [which runs the R-A4 socket-surface assert post-listen] + Linux `start_xdesktop`); both `server.rs` entries (desktop `start_server` + the Android/iOS JNI `start_server`) now call it instead of `RendezvousMediator::start_all`. The lift **drops from the service path**: `hbbs_http::sync::start()` (R-SV6(b) sysinfo POST), `test_rendezvous_server` (egress probe), `test_av1` (an `AomEncoder` startup benchmark — NOT the decoder the R-D4 prose claims; the per-session encoder instantiates on demand, AV1 then default-useable, acceptable per [[test-av1-is-encoder-not-decoder]]), and the dead empty-servers registration loop. **R-D4 lift Stage 2 done** (this commit): `rendezvous_mediator.rs` is rewritten 1155→~290 lines — the entire mediator PROTOCOL is **removed from the tree**: `start_all`, `start`/`start_tcp`/`start_udp`, `register_pk` (the method), the relay/punch-hole/intranet handlers, `create_relay`, `get_relay_server`, `start_ipv6`/`udp_nat_listen` (the UDP/KCP path incl. the `kcp_stream::KcpStream::accept`), the `Sink`, the deploy-register throttle, and `CheckIfResendPk`'s resend logic. The relay/punch/KCP worker symbols (`handle_request_relay`/`handle_punch_hole`/`udp_nat_listen`/`punch_udp_hole`/`KcpStream::accept`) are now **tree-wide absent**, a new verify.sh hard-gate. What remains is the direct-only service path (`start_direct_only`→`direct_server`), the R-A4 asserts, and **three no-op shells** kept ONLY so cross-module callers still link without a flutter build: `RendezvousMediator::restart()` (now a no-op), the `NEEDS_DEPLOY` flag + `reset_needs_deploy_notification`, and `CheckIfResendPk` (unit struct, used by `server.rs`). REMAINING — **R-D4 Stage 3** (cross-module/§19 follow-on): neuter/remove the `restart`/`NEEDS_DEPLOY` shell callers (ipc/flutter_ffi/ui_interface), gate out `mod lan`/`mod kcp_stream` (the viewer-side KcpStream::connect + lan::discover/send_wol callers), compile out `hbbs_http::sync`, and the module RENAME that finally makes `mod rendezvous_mediator` itself grep-absent (R-SV10). Note `register_pk` is not yet fully tree-absent — `ui_interface.rs set_register_pk` (the change-ID flow) survives, an R-SV5/R-G2 residual. Verified green (verify.sh EXIT 0) |
| R-SV8 iOS entitlements (APNs push + wifi-info) | **DONE** | `dd3be96`; both removed (no-phone-home), `associated-domains` confirmed absent (R-X6). macOS entitlements left (functionally required, retain-and-check); iOS `SDWebImage` pod = finding, removal pod-regen-blocked; no Firebase/analytics pod (R-SV8 holds) |
| §18 R-SV3/SV4/SV6 sovereignty — no phone-home (universal) | **PARTIAL — egress closed (three residual leaks found + fixed)** | Earlier work closed most of it: `fa56d66` deleted `do_check_software_update` (version API, R-SV3) + `test_nat_type_` (rendezvous NAT probe + `cu` re-home, R-SV4); `6920db9` emptied the rendezvous-registration loop; `56e2ad2` unspawned the HBBS sync loop; auto-updater was R-X1. **But the prior "egress CLOSED VERIFIED" claim was an OVERCLAIM — a service-entry egress audit this session found TWO live leaks it missed, now fixed:** (1) `df3d12f` — `start_all` (and every viewer process via `main.rs`/`cli`/`flutter_ffi`) still called `test_nat_type()` → `test_ipv6_sync` → `test_bind_ipv6`, which DNS-resolves `stun.l.google.com` + binds a UDP6 socket at startup; removed (`test_nat_type` is now a no-op, `test_ipv6_sync` deleted). **The dead probe BODIES the no-op left are now structurally removed** (this commit, R-SV1): `test_nat_ipv4`/`test_ipv6`/`test_bind_ipv6`/`stun_ipv4_test`/`stun_ipv6_test` + the hardcoded `STUNS_V4`/`STUNS_V6` (`stun.l.google.com`/cloudflare/nextcloud) — all dead (no callers since `test_nat_type` is a no-op, and `_start` direct-only `3ce45e1` dropped the last `test_ipv6` call) — are deleted from `common.rs`, the `stunclient` crate dep is dropped (Cargo.toml + lock −67), and a hard-gate (`R-SV4(b) common.rs STUN NAT-probes`) holds it. The other STUN source — `webrtc.rs DEFAULT_ICE_SERVERS` (cloudflare/nextcloud) — is DEAD SOURCE (the `webrtc` feature is never enabled, so the module is not compiled); removing the whole webrtc transport is an un-verifiable-here follow-on (like the Windows/sciter excisions). (2) `be727d6` — `get_api_server_` returned the hardcoded `https://admin.rustdesk.com` default, and the audit POSTs (`post_conn_audit` pre-auth, `post_file_audit`, `post_alarm_audit`) were silent ONLY via `get_audit_server`'s `is_public()` string-match (R-SV6(d) forbids exactly that); now returns `""` so they short-circuit structurally. All verify.sh-green. **(3) THIRD leak — the viewer peer-list ONLINE-STATUS query** (this commit, found auditing the post-R-P5 dead-code): `client::peer_online::query_online_states` (called by `flutter.rs` for the peer-list online dots) ran `query_online_states_` → `create_online_stream`, which `connect_tcp`'d to `get_rendezvous_server()` — **defaulting to the built-in `rs-ny.rustdesk.com`** (the same default the R-S16 `custom-rendezvous-server=""` pin does NOT override) — and sent an `OnlineRequest{ id: Config::get_id(), peers: ids }`: a **box-id + peer-list LEAK to upstream on every peer-list refresh** (the module's `#[cfg(test)]` even hard-coded real upstream IDs). The whole `peer_online` module is replaced with a no-egress stub: `query_online_states` reports every peer offline WITHOUT any network call (the egress fns `create_online_stream`/`query_online_states_` + the leaky test are deleted, −128 lines). New hard-gate `R-SV viewer online-status egress` (`create_online_stream|set_online_request` absent). flutter-verify + verify.sh green. **The flutter ONLINE-STATUS FRONTEND is also removed** (the dots commit): the peer-list `bind.queryOnlines` query triggers (`peers_view`) + the `getOnline` online/offline dot (`peer_card`/`autocomplete`) are gone — a direct-IP fork has no rendezvous server to ask, so showing an always-"Offline" status was itself incorrect; new dart-verify gate `R-G/R-D online-status query trigger`. **HTTP-EGRESS SURFACE FULLY AUDITED — dial-nobody holds**: every `post_request`/`http_request`/`create_http_client` caller is now neutered (audit/OIDC/`send_note`/`--assign` refuse-stubs), DEFANGED by the PINNED-empty `api-server` (`OPTION_API_SERVER` ∈ PINNED_SETTINGS = `""`, config.rs:3250/3312 — so `ab_model`'s `mainGetApiServer()/api/ab/...` URLs are empty-host and fail on BOTH the Rust path AND the flutter `_pollFlutterHttp` direct path), or sciter-`cfg`-gated; the `get_rendezvous_server`-CONNECT class is clean (online-status was the only live one). **R-SV7 Telegram egress now COMPILED OUT** (this commit): `send_2fa_code_to_telegram` + `get_chatid_telegram` + the `connection.rs` 2FA-gate leak site (it POSTed this box's `Config::get_id()` + the peer's source IP to a **hardcoded** `api.telegram.org`, gated on `bot`/`2fa` — NOT `api-server` — so R-D6's pin never silenced it) are removed from the tree; `verify_bot` (FFI) reports the feature absent, `has_valid_bot` keeps its pure config-read (`bot` pinned empty → always false). `api.telegram.org` is now **structurally absent** (R-SV1, not pin-safe), a new hard-gate in verify.sh (`R-SV7 Telegram 2FA egress absent`). **R-SV6(a)+(b) audit/record POST egress now SEVERED** (this commit): the three audit POSTs (`post_conn_audit` — fired PRE-AUTH in `on_open`, action:"new", leaking box id/uuid + conn/session ids; `post_file_audit` — file names/sizes; `post_alarm_audit` — whitelist/rate-limit source data) are reduced to **no-op stubs**, and the **shared reqwest sink `post_audit_async` is neutered** (no `crate::post_request`) so the egress is structurally unreachable from the audit path — not behind the empty-url pin (be727d6). `record_upload::is_enable()` is forced `false` so `run()` (the `/api/login-options` HTTP client streaming session frames out) is unreachable from its sole `video_service.rs` caller (R-SV6(b)). `crate::post_request` is gone from `connection.rs` (audit path). All verify.sh-green; the only new dead-code is the `tx_post_seq` field joining an existing never-read warning (full struct/worker symbol removal is the R-D4-era cleanup, R-SV10). **R-D4-era audit worker/sink removal now DONE** (this commit): that `tx_post_seq` field, its `post_seq_loop` worker, and the `post_audit_async` reqwest sink had ZERO send sites (the egress bodies were already gone), so the whole dead chain is STRUCTURALLY excised from `connection.rs` — the reqwest sink is GONE, not neutered (R-SV1 / R-D6 'dial nobody'). The three POSTs remain no-op stubs; their 15 call sites + `server_audit_conn`/`_file` + `get_api_server`/`get_files_for_audit` are the only audit residue (later slices). verify.sh green. **conn-audit stub + fields slice now DONE** (follow-on commit): `post_conn_audit` + its 3 call sites (close/new/peer) + `get_api_server` + the write-only `server_audit_conn`/`_file` fields are excised (the `conn_type` tuple-int that only fed the peer-audit payload dropped with it); audit residue is now `post_file_audit` (6 sites) + `post_alarm_audit` (6 sites) + `get_files_for_audit`. verify.sh green. **file-audit slice now DONE** (follow-on commit): `post_file_audit` (all 6 call sites — 3 file-transfer dir/recv handlers + 3 clipboard-`Files` arms) + `get_files_for_audit` + the orphaned `FileAuditType` enum are excised; the clipboard `Files` arms were audit-only (never forwarded), so they collapse to a consumed-no-op (behaviour-preserving). Audit residue is now just `post_alarm_audit` (6 sites). verify.sh green. **alarm-audit slice DONE — responder-audit excision COMPLETE** (follow-on commit): `post_alarm_audit` (6 call sites — the ip-whitelist block + 5 login-failure/backoff alarms) + the `AlarmAuditType` enum + the cross-file `login_failure_check.rs` machinery that produced it (`OsCredentialPolicyDecision.audit`, `backoff_audit_type_for_scope`, `block_decision`'s `alarm_type` param + the audit unit-test) are excised; the login-failure BACKOFF/blocking behaviour is fully preserved (only the audit egress is gone). ALL 4 audit POSTs + the worker/sink + both enums (`FileAuditType`/`AlarmAuditType`) are now structurally absent (R-SV1). verify.sh + login_failure_check unit tests green. **R-SV6(c) device-deploy egress EXCISED** (this commit): the `--deploy` CLI arm (core_main.rs — incl. its `is_user_main_ipc_scope_cli_command` registration + test) and `deploy_device`'s POST of `{id,uuid,pk}`+token to `get_api_server()+"/api/devices/deploy"` (account-server device registration the R-X4 `--assign`/`--set-id` excision missed) are removed; `deploy_device` is a refuse-stub keeping the flutter FFI (`main_deploy_device`) compiling, and `api/devices/deploy` is a new verify.sh hard-gate. **R-SV6(b) HBBS heartbeat/sysinfo loop now EXCISED** (this commit): `start_hbbs_sync_async` (the loop POSTing `get_sysinfo()` — hostname/username/version/uuid/preset address-book fields — to `<api-server>/api/{heartbeat,sysinfo}` and adopting server `strategy`/`disconnect`/`modified_at` from the reply) + `heartbeat_url` + `InfoUploaded` + the vestigial `start()` + **`handle_config_options` (R-X3's heartbeat re-home twin, now SYMBOL-absent, strengthening the c658967 neutralization)** are removed from `sync.rs`; only the local broadcast channel (`signal_receiver`/`is_pro`) survives. `api/heartbeat`/`api/sysinfo`/`handle_config_options` are a new verify.sh hard-gate. Confirmed `test_rendezvous_server_` is **pin-safe** (the `servers.len()<=1` guard + `RENDEZVOUS_SERVERS=["rs-ny.rustdesk.com"]` ⇒ early-return, no egress) — not a leak. REMAINING (R-SV follow-ons, all flutter/cli-gated or viewer-connect-logic — deferred with the flutter/viewer work): the GUI/cli-startup `test_rendezvous_server`/`test_nat_type` calls (`main.rs`/`cli` — cfg-gated OUT of the linux compile-check, unverifiable here); the `client.rs` viewer rendezvous/relay/KCP path + `udp_nat_connect` (R-SV4(b)/R-S13(d)) — **DONE** (`3ce45e1`+`fbddedd`: `_start` direct-only, the cluster deleted) — and its peer-list **online-status leak DONE** (`cebfdf2`); the `get_rendezvous_server`-CONNECT egress class is now fully audited (online-status was the only one — the other `get_rendezvous_server` reads are config/proxy-decision/string-construction, NOT connects). account egress (was all defanged by the empty `get_api_server`, be727d6 — dead-not-live; the R-G4 account-system sweep now removes it structurally per R-SV1): the **`hbbs_http::account` OIDC is DONE** (this commit) — `account.rs` rewritten 366→162 lines, the OIDC egress (`auth_task` → POST `/api/oidc/auth` with a `deviceInfo` fingerprint, poll `/api/oidc/auth-query`, warm `/api/login-options`) + its querying state machine deleted, `OidcSession::account_auth` a refuse-stub with NO network call (the FFI/ui_interface API + the `AuthResult`/`AuthBody` serialization shape preserved); new hard-gate `R-SV6(b)/R-G4 OIDC account-login egress`. **`send_note` + `--assign` also DONE** (this commit): `Session::send_note` no longer POSTs the note to the audit server (the free `send_note` -> `post_request` is deleted; the note is kept locally), and the `--assign` `/api/devices/cli` account device-assignment POST is excised (a refuse-stub print), folded into the `R-SV6(c) device-deploy/assign egress` gate. So the Rust account egress is fully severed; REMAINING is the flutter account/login UI (R-G4 §19); and the `mod lan` removal + the `rendezvous_mediator` shell-caller neutering/module-rename (R-D4 Stage 3); **`mod kcp_stream` is now REMOVED** (the dead KCP/relay transport — direct-IP-only never uses it: the viewer connect-tuple's always-`None` `Option<KcpStream>` slot dropped across `_start`/`start`/io_loop/port_forward, the dead `if kcp.is_some()` close-reason branch deleted, and `src/kcp_stream.rs` (151 lines) gone; R-SV1, verify.sh green). R-D5 v4-only bind done (`b6df149`). **Egress audit + R-G2 step 1 (this commit).** EGRESS AUDIT — R-D6 'dial nobody' VERIFIED on the flutter `--server`: swept ALL outbound calls (http.get/post/put, reqwest, WebSocket, Socket.connect, post_request); the only residual flutter egresses are the account address-book/group SYNC (`ab_model.dart` ~15 + `group_model.dart` 3 `http.*`), EVERY one gated behind `userModel.isLogin` (always false on a direct-IP fork ⇒ unreachable), and the Rust `post_request` reqwest sink is fed only by the sciter `ui_interface` FFI (R-B6, uncalled on flutter). With the already-removed+gated egresses (audit http.get/put, telegram, devices/deploy, HBBS heartbeat, online-status, version-check), NO outbound call fires on the controlled box. The residual egress CODE is removed structurally with R-G2 (the account subsystem, ~162 abModel refs: address_book.dart 54, peer_card.dart 36, peer_tab_page.dart 34, dialog.dart 14, peers_view/model/main/user_model) + R-D4/R-B6 (sciter post_request). **R-G2 STEP 1:** `autocomplete.dart` de-account'd — `AllPeersLoader` no longer merges `abModel`/`groupModel` peers (the connect-page suggestion list is now local Discovered+Recent only) and the `AutocompletePeerTile` AB-tag-color badge is gone; 8 abModel/groupModel refs removed, dart-verify green. **STEP 2:** main.dart (the two startup `Future.wait([abModel.loadCache, groupModel.loadCache])` calls — empty caches on a direct-IP fork) + model.dart (the dead `sync_peer_hash_password_to_personal_ab` AB-sync event handler) de-account'd — main.dart is now AB/group-free, and model.dart's only remaining abModel ref is the field init; dart-verify green. The woven cascade (peer_card AB tags, the peer_tab_page tab-index rework, address_book/my_group widgets + their peers_view views, the dialog AB refs, then the models + login.dart) is the next focused step — all DEAD UI (the AB/group tabs are off), so removal is behavior-equivalent. |
| §19 R-G* GUI/UX conformance (remove selectors/toggles/dead assets/links the core no longer honors) | **PARTIAL — sweep STARTED + Dart-verifiable** | The Dart side is now verifiable (`scripts/dart-verify.sh`, `flutter analyze lib/` GREEN + a §19 Dart-layer grep gate), so the sweep is gate-backed. **R-G3 done** (this commit): the session security badge could render an "insecure"/"relayed" state — both DEAD (the fork is always §10-PAKE-keyed + R-SV4/R-D4 direct-only) and a dangerous security MISLABEL. Collapsed: `getConnectionText` (common.dart) → the one reachable "Direct and encrypted" text (3 dead branches deleted); the badge `SvgPicture.asset` in remote_tab_page + view_camera_tab_page → hardcoded `secure.svg` (can't structurally mislabel); the `insecure.svg`/`secure_relay.svg`/`insecure_relay.svg` assets deleted; new dart-verify gate (`R-G3 insecure/relay badge assets absent`). **R-G4 startup version-check trigger gutted** (`75ccea5`): `checkUpdate()` (common.dart) registered a `kCheckSoftwareUpdateFinish` handler + called `bind.mainGetSoftwareUpdateUrl()` 1 s after launch (the api.rustdesk.com/version FFI) — gutted, so the app makes NO version-check call at startup (completes R-SV3 on the Dart side: the egress was already gutted in Rust, fa56d66; this killed the trigger). Gate (`R-G4/R-SV3 startup version-check FFI trigger absent`). **R-G4 dead update GUI REMOVED** (this commit): with the trigger gone `stateGlobal.updateUrl` is never set, so the whole update UI was dead — now DELETED: the desktop update card (`buildHelpCards`, which also linked to rustdesk.com/download + a github.com/rustdesk release tag — R-G8), the mobile `_buildUpdateUI` banner, the **`UpdateProgress` downloader widget** (`update_progress.dart` deleted entirely — it issued the `download-new-version`/`update-me` FFI, the actual R-X1 fetch-and-run egress), the check-update/auto-update **settings toggles** (mobile settings_page + desktop_setting_page), `checkUpdate()` itself + BOTH its `main.dart` callers, and the now-orphaned `kOptionEnableCheckUpdate`/`kOptionAllowAutoUpdate` Dart consts. `flutter analyze lib/` GREEN (0 errors); three new gates (`R-G4 dead update widgets` / `update-toggle option keys` / `update-UI strings` absent). **R-G4 Rust update-option consts REMOVED** (this commit): `config.rs` `OPTION_ENABLE_CHECK_UPDATE` (orphaned) and `OPTION_ALLOW_AUTO_UPDATE` (also dropped from `KEYS_SETTINGS`, so a DEFAULT/OVERWRITE config can no longer set the removed feature) are deleted — verify.sh green. REMAINING R-G4 update residue: the `main_get_software_update_url`/`main_update_me` Rust FFI (+ any Dart callers — `main_update_me` is the local lower-version reinstall, no egress) and the dead `src/lang/*.rs` update strings. **Audit/note egress + Note menu REMOVED (this commit, R-SV/R-G4):** `model._queryAuditGuid` did a live `http.get` to `<audit-server>?id=<peer>&session_id=…&conn_type=…` on every `handlePeerInfo` — a latent peer-id/metadata egress (pin-gated: `get_audit_server` returns empty when the api-server is empty, but R-SV1 wants the egress CODE gone, not merely pinned). It + its caller, the toolbar "Note" `TTextMenu`, and `showAuditDialog` (its sole caller) are deleted; dart-verify green. **Note-at-close flow REMOVED (this commit) — the audit feature's 2nd egress is closed.** The note-at-close dialog `_showConnEndAuditDialogCloseCanceled` carried a live `http.put` of the note to `<audit-server>` (latent: gated on the never-fetched audit GUID). Deleted the whole flow: the 13 tab-close guards (file_manager/terminal/remote/view_camera tab pages) + the 5 other callers (model.dart `showMsgBox` + the relay-hint/pi-close handler, dialog.dart `clientClose`, the tab wrapper, remote_toolbar.dart close menu) restructured to the plain close path, and the defs (`allowAskForNoteAtEndOfConnection`, `showConnEndAuditDialogCloseCanceled`, `_showConnEndAuditDialogCloseCanceled`, `desktopTryShowTabAuditDialogCloseCancelled`, `buildNoteTextField`) + the `kOptionAllowAskForNoteAtEndOfConnection` settings (desktop+mobile + the mobile state field) all removed. dart-verify green. **Flutter audit FFI REMOVED (this commit):** the 5 now-uncalled `flutter_ffi` exports (`session_get_audit_server_sync`/`session_send_note`/`session_get_last_audit_note`/`session_set_audit_guid`/`session_get_audit_guid`) + their `web/bridge.dart` stubs + the orphaned `http_service` egress import in dialog.dart are deleted (FRB regen + flutter-verify + dart-verify green; the `ui_session_interface` audit methods stay — sciter R-B6). So the ENTIRE Flutter audit feature (UI + both http egresses + FFI) is now gone. REMAINING (audit follow-on): the connection.rs responder audit is now FULLY EXCISED — R-D4-era audit cleanup COMPLETE: the `post_audit_async` sink + `tx_post_seq`/`post_seq_loop` worker; `post_conn_audit` + 3 sites + `get_api_server` + `server_audit_conn`/`_file`; `post_file_audit` + 6 sites + `get_files_for_audit` + `FileAuditType`; AND `post_alarm_audit` + 6 sites + `AlarmAuditType` + the `login_failure_check.rs` audit-type machinery — all REMOVED (verify.sh + login_failure_check unit tests green); the sciter `get_audit_server` (header.tis/remote.rs) is R-B6. REMAINING (the large Dart sweep): R-G2 (ID-centric home → direct-address model, R-SV5), R-G4 (account/OIDC, 2FA, OTP board, ID/Relay+SOCKS editors, Direct-IP toggle, the os-username/os-password + Request-Elevation UI feeding the R-S18 senders). **Approve-mode selector REMOVED** (this commit, R-G4/R-S9): the desktop _Safety "Accept sessions via password/click/both" `ComboBox` is dead — approve-mode is pinned `password` in PINNED_SETTINGS (config.rs:3204), so the selector was rendered disabled/greyed (`isApproveModeFixed`); removed it + its `modeKeys`/`modeValues`/`modeInitialKey` data (the `usePassword` flag stays, driven by the pinned value, so the password UI below is unchanged). **Desktop password-_Card DE-CLUTTERED** (follow-on commit): the `password()` method's verification-method radios (`use-temporary`/`use-permanent`/`use-both`, pinned `use-permanent-password`) + the one-time-password UI (the OTP length radios + the numeric-OTP checkbox, R-X7-excised) are removed — the desktop _Safety "Password" card now shows only the permanent-password setup (the fork's sole credential, R-P1/R-S9); dart-verify green. The mobile `server_page` password popup-menu is ALSO de-cluttered (follow-on commit): the accept-mode/OTP/verification-method `PopupMenuItem`s + their `onSelected` handlers + the `listTile` helper + the dead vars are removed — the menu is now just "Change ID" + "Set permanent password". So both flutter frontends show only the permanent-password setup. The `ServerModel` methods (`setVerificationMethod`/`setTemporaryPasswordLength`/`switchAllowNumericOneTimePassword`/`setApproveMode`) are REMOVED (follow-on commit) from `server_model.dart`, along with the now-dead `setTemporaryPasswordLengthDialog` (the OTP-length dialog, dialog.dart) and its only-caller `_showSuccess` helper. So the entire flutter verification-method/approve-mode/OTP password-config surface (UI → model setters → dialogs) is gone; the password getters (`verificationMethod`/`approveMode`/etc., read by the password board) stay. REMAINING: the sciter password/verification selectors (R-B6) — **OIDC SSO provider-login slice DONE** (this commit): login.dart's `_IconOP`/`ButtonOP`/`WidgetOP`/`LoginWidgetOP`/`ConfigOP` + `kOpSvgList` + `kAuthReqTypeOidc` + the `loginDialog` third-auth section + `UserModel.queryOidcLoginOptions` + the 10 `auth-*.svg` provider icons are removed (providers were enumerated from the pinned-empty api-server ⇒ always-empty `loginOptions` ⇒ the section rendered Offstage-dead); new dart-verify gate `R-G4 OIDC SSO provider-login widgets`. The username/password account login + `UserModel` + the address-book/group account coupling remain (the rest of the account sweep). **Desktop "Network" settings tab REMOVED** (this commit): the `_Network`/`_NetworkState` classes (the "ID/Relay Server" editor + the Socks5/Http(s) proxy + the WebSocket switch — config UI for the rendezvous/relay/api infrastructure the fork structurally removed, all dead/pinned) + the `SettingsTabKey.network` enum value + its `tabKeys` include + both `_settingTabs()`/`_children()` switch cases; new dart-verify gate `R-G4 desktop Network/server-config settings tab`. **Mobile Network removal DONE** (this commit): the mobile `settings_page` "ID/Relay Server" + "Socks5/Http(s) Proxy" `SettingsTile`s + their orphaned `_hideServer`/`_hideProxy` state, plus the now-uncalled shared `changeSocks5Proxy` (the proxy-editor dialog, desktop_setting_page) and `showServerSettings` (mobile/widgets/dialog.dart) — both removed; the gate is widened to `SettingsTabKey.network|changeSocks5Proxy|void showServerSettings(`. Still standing (separate follow-ons): `showServerSettingsWithValue` (used by the QR `scan_page` server-add, R-G2-coupled), the mobile "Use WebSocket"/"Deploy" tiles, and the now-uncalled `mainGetSocks`/`mainSetSocks`/`mainTestIfValidServer`/`mainIsUsingPublicServer` FFI exports (a flutter-verify FFI-trim follow-on, like the trusted-devices FFI). **Account sweep STARTED — desktop "Account" settings tab REMOVED** (this commit, R-G4/R-SV6): the `_Account`/`_AccountState` classes (the rustdesk-account login/logout panel) + the `SettingsTabKey.account` enum value + its `tabKeys` include + both `_settingTabs()`/`_children()` switch cases; new dart-verify gate `R-G4 desktop Account settings tab`. (`is_disable_account()` = `is_some_hard_opton("disable-account")` is NOT pinned, so the account is currently *enabled* — the spec wants it compiled out, R-SV6, not merely gated.) **Note-feature login-gates DROPPED** (this commit, R-G4 correctness): the "ask for note at end of connection" feature is local (R-SV severed `send_note`'s POST), but its three frontends gated the option on `loginDialog()` succeeding — and on a direct-IP fork login can never succeed, so the note option was effectively **un-settable**. Removed the dead login-gate from all three (the desktop `optSetter`, the mobile toggle `onToggle`, and the toolbar "Note" menu `onPressed` → straight to `showAuditDialog`) + the 2 now-unused `login.dart` imports (desktop_setting_page/toolbar). `loginDialog` now has 2 callers left. REMAINING account sweep: `loginDialog`'s last 2 settings/web callers REMOVED (a follow-on commit): the mobile Settings>Account `SettingsSection` (the avatar + Login/Logout tile) and the web `context['loginDialog']` exposure, plus their now-unused `login.dart` imports. **`loginDialog`'s only remaining callers are now the R-G2-coupled `my_group.dart`/`address_book.dart` "Login" buttons** — on a direct-IP fork `userModel.isLogin` is always false, so those two widgets ALWAYS render a dead "Login" button and the group/address-book content (the `else` branch) is unreachable: the address book is currently *inaccessible*. **R-G2 first step DONE (this commit): the Address Book + "Accessible devices"/group tabs are hardcoded structurally OFF in `peer_tab_model.isEnabled` (constant `false`, no longer gated on the flippable `disable-ab`/`disable-account` runtime config) — so the broken account tabs no longer render and the user lands on the local Recent/Favorites/Discovered peer lists (R-SV5); the inaccessibility bug is fixed, dart-verify green.** The full structural compile-out remains (a dedicated multi-commit follow-on): the index-based tab system (`maxTabCount`=5 + the parallel `tabNames`/`icons`/`isEnabled`/`orders` arrays + the saved `kOptionPeerTabVisible/Order` decode) must drop to 3, then the `AddressBook`/`MyGroup` entries + widgets, then `abModel` (~170 refs woven through peer_card/peers_view/autocomplete/dialog/model), `groupModel`, `userModel`+login.dart, and the `is_disable_*` FFI. The R-G2/R-SV5 end-state is to make the address book personal-only (keyed `<ip|domain>:port`, shown without login), which then drops the "Login" buttons, `loginDialog`, all of login.dart (`LoginWidgetUserPass`/`verificationCodeDialog`/`logOutConfirmDialog`), and the now-account-only `Dialog2FaField` (dialog.dart, used only by verificationCodeDialog). Until then the leftover login.dart symbols are: `LoginWidgetUserPass`/`verificationCodeDialog`/`logOutConfirmDialog` (login.dart), `UserModel` (user_model.dart), and the address-book/group account de-coupling (personal-only) — the last is R-G2-coupled. **Dead "Plugin" settings tab REMOVED** (this commit, R-X2/R-G4): the native-plugin loader is already excised (R-X2 — `mod plugin`/`plugin_framework` absent) and `plugin_feature_is_enabled()` is pinned `SyncReturn(false)`, so the `_Plugin`/`_PluginState` settings tab was always hidden + dead — removed (the classes + `SettingsTabKey.plugin` enum value + its `tabKeys` include + both switch cases), which also drops the second `loginDialog` caller (the "login to use plugins" button); new gate `R-X2/R-G4 dead Plugin settings tab`. So the desktop settings now has 5 tabs (General/Security/Display/Printer/About) — Network/Account/Plugin all gone. The account FFI stubs are a flutter-verify FFI-trim follow-on. **FFI-trim DONE for the dead editors/tabs** (this commit): the 4 now-uncalled `flutter_ffi` exports `main_get_socks`/`main_set_socks` (the removed SOCKS editor), `plugin_feature_is_enabled` (the removed Plugin tab), and `session_send2fa` (the removed 2FA dialog) + their `web/bridge.dart` stubs are removed; FRB Rust-bridge regen + cargo check (flutter-verify) and the dart bridge regen + analyze (dart-verify) both green. `main_test_if_valid_server` is KEPT (still 2 callers in common.dart — server-config validation via `showServerSettingsWithValue`/`scan_page`); `send2fa`/`get_socks`/`set_socks` (ui_interface/ui_session_interface backends) stay for the Sciter UI (R-B6), R-G5 — **DONE** (`b0eae8a`/`b99fe49`): the R-S17 known-hosts seed dialog (`hostNotPinnedDialog`) + the pre-keying password dialog (`enterConnectPasswordDialog`) — GUI onboarding of a brand-new peer is now end-to-end (a mismatch stays a loud no-bypass error; a dedicated mismatch panel + a manage/forget-hosts view are optional polish over the `--forget-host`/`--list-known-hosts` CLI), R-G6 (relay-fallback error copy + WoL), R-G7 (CM accept + Android overlay), R-G8 — **mostly DONE** (`2978284`): every user-facing rustdesk.com link removed (the "powered by" badge + About/Website links, the Privacy/EULA `privacy.html` links, the macOS/Linux permission-card docs "Help" links), new dart-verify gate `R-G8 rustdesk.com privacy/docs links`; only `rustdesk.com/pricing` in the dead `onUsePublicServerGuide` remains (goes with R-G2's server-UI removal), and the **online-status dots/query are also removed** (`135cc25`, R-G/R-D), and the dead `lang/` keys |
| R-R3 dependency audit (Appendix D bumps) | **PARTIAL — 4 code-vulns FIXED, `cargo audit` GREEN via deny.toml** | `5ef7cfe`: in-range lockfile bumps clear **4 code-vulns** on the pinned 1.75 toolchain (all compile-verified lockdown on+off, crypto suite green) — **url 2.5.4** (idna 0.5.0→1.0.3, RUSTSEC-2024-0421; pinned `idna_adapter 1.1.0` unicode-rs backend to dodge icu 2.x/edition2024, ~20 crates lighter), **rand 0.8.6** (2026-0097, the `thread_rng` UB in the CSPRNG for password/salt + CPace scalars), **crossbeam-channel 0.5.15** (2025-0024 double-free), **tracing-subscriber 0.3.20** (2025-0055). New **`deny.toml`** records **41 conscious accepts** each with a reason (R-R3's "ignore + reason"); **cargo-audit 0.21.1** (the last rustc-1.75 line) returns **EXIT=0** against a pinned pre-2026 advisory-db — green, zero unignored. The wired audit surfaced more than the offline Appendix D screen (openssl 2025-0004, crossbeam, tracing-subscriber, libgit2-sys), exactly as the spec predicted. The **Rust audit gate is WIRED** (`4e118ee`): `scripts/audit.sh` + `scripts/Dockerfile.audit` run cargo-audit 0.21.1 against a pinned advisory-db (commit `4ea955ae`, in `pins.env`) with the deny.toml accepts, fail-closed — `bash scripts/audit.sh` returns green. REMAINING: the **fork-bumps** (bindgen 0.59→0.69 clears atty+ansi_term; users→uzers — both need editing the rustdesk-org git-dep forks), the **openssl** pair (fix needs openssl-sys/rustc 1.80, colliding with the R-B5a 1.75 pin — a real toolchain-vs-advisory tension), **time** macOS-prune (with R-R2), the **Dart `osv-scanner`** half of the gate, and the `sdp`/`webrtc` dev-dep `is_multiple_of`/1.75 bump (worked around via the isolated test crates). |

## Roadmap for a build-capable continuation

1. Stand up the build env (the `scripts/` harness, R-B7 baseline parity first).
2. **De-tangle in order:** R-B6 Sciter (make flutter default; drop `sciter-rs` with a
   lock regen) → R-D4 mediator (extract `direct_server`→`start_direct_only`) → then
   R-X1/R-X3/R-X4/R-X5/R-X11/R-F4 fall out near-self-contained.
3. Implement `libs/pake` against the §10.4 KAT anchors; wire it + the two-key
   `secretbox` rewrite + the choke point (R-P14); run the KAT/negative suite.
4. Land R-S16 `PINNED_SETTINGS` and the §9 assertions; make the R-A6 CI greps green.
5. R-R2b lockdown-feature retirement (**DONE**) + the R-S16(d)(ii) SwitchPermission-widener removal (**DONE**); R-R3 audit (fork-bumps), §19 GUI sweep, R-R2 CI prune, smoke-tests (R-B4) remain.

The commit history is the per-change record (each message cites its requirement and
what it defers); this file is the per-requirement overview.
