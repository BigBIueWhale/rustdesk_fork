<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carriez.flutter_hbb">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- R-X6: READ/WRITE_EXTERNAL_STORAGE dropped — superseded by MANAGE_EXTERNAL_STORAGE
         on the pinned targetSdk 33; requestLegacyExternalStorage removed below. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- R-X6: SYSTEM_ALERT_WINDOW dropped with the FloatingWindowService cut — a
         single-purpose box needs no overlay/tapjacking primitive; controlled status
         is shown by the mandatory foreground-service notification. -->


    <queries>
        <intent>
            <!-- https://developer.android.com/training/package-visibility/use-cases#open-urls-custom-tabs -->
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="RustDesk"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"><!-- R-X6: allowBackup=false (the UUID-obfuscated
        config holds the password-equivalent PRS — block adb-backup exfil);
        networkSecurityConfig denies cleartext (defense-in-depth, §18); the upstream
        requestLegacyExternalStorage is removed (targetSdk 33 scoped storage). -->

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <!-- R-X6: exported=false (BOOT_COMPLETED is a protected system broadcast,
                 still delivered to a non-exported receiver) and the upstream
                 debug-boot test broadcast action removed, so no installed app can
                 fake a boot to auto-start the controlled side. -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".InputService"
            android:enabled="true"
            android:exported="false"
            android:label="RustDesk Input"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode|navigation"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent for deep linking-->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="rustdesk" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PermissionRequestTransparentActivity"
            android:excludeFromRecents="true"
            android:theme="@style/Transparent" />

        <service
            android:name=".MainService"
            android:enabled="true"
            android:foregroundServiceType="mediaProjection" />

        <service
            android:name=".NativeVideoDecoderService"
            android:enabled="true"
            android:exported="false"
            android:isolatedProcess="true" />

        <service
            android:name=".NativeAudioDecoderService"
            android:enabled="true"
            android:exported="false"
            android:isolatedProcess="true" />

        <service
            android:name=".NativeZstdDecoderService"
            android:enabled="true"
            android:exported="false"
            android:isolatedProcess="true" />

        <!--
 Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java
        -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>
