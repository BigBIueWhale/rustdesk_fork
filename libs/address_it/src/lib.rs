//! No library code — this crate exists only to host the R-SV5 address-
//! normalization KAT in `tests/`, built against hbb_common's library (so the
//! broken `webrtc`/`sdp` dev-dep is excluded). See `tests/address.rs`.
