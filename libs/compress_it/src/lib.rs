//! No library code — this crate exists only to host the R-S7 decompression-bound
//! unit tests in `tests/`, built against hbb_common's library (so the broken
//! `webrtc`/`sdp` dev-dep is excluded). See `tests/compress.rs`.
