//! No library code — this crate exists only to host the R-S16 lockdown funnel
//! integration test in `tests/`, built against hbb_common with the `lockdown`
//! feature on (library only, so the broken `webrtc`/`sdp` dev-dep is excluded).
//! See `tests/lockdown.rs`.
