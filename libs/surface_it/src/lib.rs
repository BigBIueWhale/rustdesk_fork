//! No library code — this crate exists only to host the R-A4 socket-surface
//! parser/policy unit tests in `tests/`, built against hbb_common's library
//! (so the broken `webrtc`/`sdp` dev-dep is excluded). See `tests/surface.rs`.
